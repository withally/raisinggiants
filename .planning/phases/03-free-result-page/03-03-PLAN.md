---
phase: 03-free-result-page
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - components/result/CTABlock.tsx
  - components/result/StickyMobileCTA.tsx
  - components/result/ResultShell.tsx
autonomous: false
requirements: [RSLT-05]

must_haves:
  truths:
    - "A purchase CTA appears after the hero section (catches impulse buyers)"
    - "A purchase CTA appears at the bottom after all teaser sections"
    - "CTA button text is 'Unlock Your Complete Guide' with $14 price displayed nearby but not on the button"
    - "A credibility line appears near the CTA: research-backed trust signal"
    - "Sticky CTA bar is visible on mobile at the bottom of the screen as user scrolls"
    - "Sticky CTA bar has safe-area padding for iPhone notch"
    - "CTA links to /checkout?session=<sessionId> (placeholder for Phase 4)"
    - "All 5 RSLT requirements are satisfied on the complete page"
  artifacts:
    - path: "components/result/CTABlock.tsx"
      provides: "Purchase CTA with button, price, and credibility line"
      min_lines: 25
    - path: "components/result/StickyMobileCTA.tsx"
      provides: "Fixed bottom bar for mobile with safe-area padding"
      min_lines: 20
  key_links:
    - from: "components/result/CTABlock.tsx"
      to: "/checkout?session=*"
      via: "Placeholder href for Phase 4 Stripe integration"
      pattern: "/checkout\\?session="
    - from: "components/result/StickyMobileCTA.tsx"
      to: "/checkout?session=*"
      via: "Same placeholder checkout href"
      pattern: "/checkout\\?session="
    - from: "components/result/ResultShell.tsx"
      to: "components/result/CTABlock.tsx"
      via: "Two CTABlock placements (after hero and at bottom)"
      pattern: "CTABlock"
---

<objective>
Add purchase CTA blocks (early and final) with trust signals, and a sticky mobile CTA bar. Then complete a human verification of the full result page against all 5 RSLT requirements.

Purpose: RSLT-05 requires a single clear CTA to purchase the full PDF guide at $14. This plan adds two inline CTA placements plus a sticky mobile bar, ensuring the purchase path is always accessible. The checkpoint verifies the complete page experience.

Output: Complete result page with all 5 RSLT requirements implemented and human-verified.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-free-result-page/03-RESEARCH.md
@.planning/phases/03-free-result-page/03-01-SUMMARY.md
@.planning/phases/03-free-result-page/03-02-SUMMARY.md
@components/result/ResultShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CTABlock and StickyMobileCTA components</name>
  <files>
    components/result/CTABlock.tsx
    components/result/StickyMobileCTA.tsx
    components/result/ResultShell.tsx
  </files>
  <action>
Create `components/result/CTABlock.tsx`:

Props:
```ts
interface CTABlockProps {
  sessionId: string
}
```

Layout (centered, within max-w-2xl container):
1. **Credibility line**: "Based on research from 100+ leading parenting experts" in `text-sm text-stone-500 text-center mb-3`
2. **CTA button**: Large, prominent button with text "Unlock Your Complete Guide". Style: `bg-amber-600 hover:bg-amber-700 text-white font-semibold py-4 px-8 rounded-xl text-lg w-full sm:w-auto transition-colors`. Link to `/checkout?session=${sessionId}` using Next.js `Link` component. This is a placeholder URL — Phase 4 will create the actual checkout page.
3. **Price**: Display "$14" prominently below the button in `text-lg font-medium text-stone-700 text-center mt-2`. Add a subtitle: "One-time purchase — instant PDF delivery" in `text-sm text-stone-500`.

Wrap in a `py-8` section. The overall vibe: warm, confident, not pushy. The button should feel like the natural next step.

Create `components/result/StickyMobileCTA.tsx`:

Props:
```ts
interface StickyMobileCTAProps {
  sessionId: string
}
```

Layout:
- `fixed bottom-0 left-0 right-0` container, only visible on mobile: `block sm:hidden`
- Background: `bg-white/95 backdrop-blur-sm border-t border-stone-200`
- Padding: `px-4 py-3` with safe-area for iPhone notch: `pb-[max(0.75rem,env(safe-area-inset-bottom))]`
- Content row: Flex layout with price on the left ("$14" in font-semibold) and compact CTA button on the right ("Unlock Guide" — shorter text for mobile bar)
- Button links to `/checkout?session=${sessionId}`
- Add a `z-50` to ensure it floats above all content

Wire into `ResultShell.tsx`:
- **Early CTA**: Render `CTABlock` immediately after `ResultHero` (before TransmissionSection)
- **Final CTA**: Render `CTABlock` at the very bottom of the page (after PartnerTeaser)
- **Sticky Mobile**: Render `StickyMobileCTA` once, outside the main content flow

Add bottom padding to the main content area on mobile (`pb-20 sm:pb-0`) to prevent the sticky bar from overlapping the final content.

Run `npx biome check --write components/result/` after creating files.
Run `npm run build` to verify.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npm run build 2>&1 | tail -20</automated>
    <manual>Navigate to /result?session=valid-uuid on mobile viewport — verify sticky CTA bar visible at bottom</manual>
  </verify>
  <done>Two CTABlock placements (after hero + bottom), sticky mobile bar with safe-area padding, all linking to placeholder /checkout?session=; RSLT-05 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete result page</name>
  <files>components/result/ResultShell.tsx</files>
  <action>
Human verification checkpoint. What was built:

Complete free result page with all 5 RSLT requirements:
- RSLT-01: Archetype name + teaser summary (ResultHero)
- RSLT-02: Blurred/partial PDF preview sections (TeaserSection x2)
- RSLT-03: Transmission mechanism explaining upbringing-to-archetype connection (TransmissionSection)
- RSLT-04: Partner dialogue teaser with locked guide response (PartnerTeaser)
- RSLT-05: Purchase CTA at $14 with credibility line (CTABlock x2 + StickyMobileCTA)

How to verify:

1. Start the dev server: `npm run dev`
2. Complete the quiz flow end-to-end (or use a previously completed session ID in the URL)
3. On the /result page, verify:

**RSLT-01 — Archetype display:**
- [ ] Archetype name is large and prominent (first thing you see)
- [ ] Illustration placeholder shows a colored circle with icon
- [ ] Secondary blend mentioned ("with shades of...")
- [ ] Tagline shown in italic
- [ ] 2-3 sentence teaser summary visible
- [ ] Fade-in-up animation plays on load

**RSLT-02 — PDF preview teasers:**
- [ ] "Foundational Patterns" section shows first theme clearly
- [ ] Remaining themes are faded/blurred with gradient overlay
- [ ] "Watchouts" section follows same partial reveal pattern
- [ ] Both sections framed as PDF chapter previews ("In your full guide:")
- [ ] Locked content cannot be selected (select-none)

**RSLT-03 — Transmission mechanism:**
- [ ] Section explains how upbringing shaped the archetype
- [ ] Research anchor sentence is present and reads naturally
- [ ] If cultural background was provided: cultural overlay text appears and adds meaningful context
- [ ] If cultural background was NOT provided: section is clean, no empty/placeholder content

**RSLT-04 — Partner dialogue teaser:**
- [ ] 2-line conversation snippet is visible (partner + your response)
- [ ] Third line (guide response) is locked behind "See your blueprint" hook
- [ ] The dialogue feels relatable and makes you want to read more

**RSLT-05 — Purchase CTA:**
- [ ] CTA button says "Unlock Your Complete Guide"
- [ ] $14 price displayed prominently near button (not on it)
- [ ] Credibility line visible near CTA
- [ ] Early CTA appears right after the hero section
- [ ] Final CTA appears at the bottom after all teasers
- [ ] On mobile: sticky CTA bar visible at bottom of screen
- [ ] CTA links to /checkout?session=... URL

**Overall feel:**
- [ ] Page feels like a personalized letter, not a sales page
- [ ] Continuous scroll — no hard card breaks between sections
- [ ] Warm, intimate color palette (amber/stone tones)
- [ ] Mobile-responsive layout works well on phone viewport
- [ ] The page makes you want to unlock the full guide

Resume signal: Type "approved" to complete Phase 3, or describe issues to fix.
  </action>
  <verify>Human confirms all RSLT-01 through RSLT-05 checkboxes pass</verify>
  <done>All 5 RSLT requirements verified by human; Phase 3 complete; ready for Phase 4 (Payment Flow)</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. CTA button links to /checkout?session=<sessionId> (placeholder — Phase 4 will wire Stripe)
3. Two CTA placements visible (after hero + bottom)
4. Sticky mobile CTA bar visible only on mobile viewports
5. iPhone safe-area padding works (no overlap with home indicator)
6. Human verification confirms all 5 RSLT requirements satisfied
7. `npx biome check components/result/` passes clean
</verification>

<success_criteria>
- RSLT-05 satisfied: clear purchase CTA with $14 price, credibility line, and two placements
- Sticky mobile CTA always accessible during scroll
- Human verification confirms all 5 RSLT requirements (RSLT-01 through RSLT-05) pass
- Complete result page creates genuine purchase intent through warm personalization and strategic teasers
- Phase 3 is complete and ready for Phase 4 (Payment Flow)
</success_criteria>

<output>
After completion, create `.planning/phases/03-free-result-page/03-03-SUMMARY.md`
</output>
