---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260224000000_phase1_data_foundation.sql
  - supabase/seed.sql
  - lib/supabase/client.ts
  - lib/supabase/server.ts
  - .env.local.example
  - package.json
autonomous: true
requirements: []
user_setup:
  - service: supabase-cli
    why: "Local Supabase instance for development and migration management"
    env_vars: []
    dashboard_config:
      - task: "Install Supabase CLI if not already installed"
        location: "Terminal: brew install supabase/tap/supabase (macOS) or npm i -g supabase"

must_haves:
  truths:
    - "quiz_sessions table exists with all locked columns: id, user_id, email, status, answers, dimension_scores, archetype_id, cultural_background, started_at, completed_at, created_at, updated_at"
    - "orders table exists with all locked columns: id, quiz_session_id, user_id, stripe_checkout_session_id, status, amount_cents, customer_email, pdf_storage_path, paid_at, fulfilled_at, delivered_at, created_at, updated_at"
    - "RLS is enabled on both tables with INSERT/SELECT/UPDATE policies on quiz_sessions and SELECT-only for orders"
    - "blueprints storage bucket exists and is private"
    - "Anonymous sign-in is enabled in local Supabase config"
    - "Browser and server Supabase client helpers exist"
  artifacts:
    - path: "supabase/migrations/20260224000000_phase1_data_foundation.sql"
      provides: "Complete DDL for quiz_sessions, orders, RLS policies, indexes, triggers, storage bucket"
      contains: "CREATE TABLE quiz_sessions"
    - path: "supabase/config.toml"
      provides: "Local Supabase configuration with anonymous auth enabled"
      contains: "enable_anonymous_sign_ins = true"
    - path: "lib/supabase/client.ts"
      provides: "Browser Supabase client using anon key"
      exports: ["createClient"]
    - path: "lib/supabase/server.ts"
      provides: "Server-side Supabase admin client using service_role key"
      exports: ["createAdminClient"]
  key_links:
    - from: "supabase/config.toml"
      to: "supabase/migrations/"
      via: "supabase start applies migrations from this directory"
      pattern: "enable_anonymous_sign_ins"
    - from: "lib/supabase/client.ts"
      to: "NEXT_PUBLIC_SUPABASE_URL"
      via: "environment variable injection"
      pattern: "createBrowserClient"
    - from: "lib/supabase/server.ts"
      to: "SUPABASE_SERVICE_ROLE_KEY"
      via: "environment variable injection (server-only, no NEXT_PUBLIC_ prefix)"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Initialize the Supabase project structure and create the complete database schema migration with RLS policies, anonymous auth configuration, storage bucket, and Supabase client helper files.

Purpose: Establish the database and infrastructure foundation that all downstream phases (quiz engine, payment, PDF delivery) build against. Every schema decision from CONTEXT.md must be implemented exactly to prevent cascading rework.

Output: Supabase project directory with migration SQL, config.toml, client helpers, and environment variable template.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Supabase project and create complete migration SQL</name>
  <files>
    supabase/config.toml
    supabase/migrations/20260224000000_phase1_data_foundation.sql
    supabase/seed.sql
    .env.local.example
  </files>
  <action>
1. Run `npx supabase init` to scaffold the `supabase/` directory (creates config.toml, migrations/, seed.sql).

2. Edit `supabase/config.toml` — enable anonymous sign-ins:
   - Find the `[auth]` section
   - Set `enable_anonymous_sign_ins = true`
   - Keep all other defaults

3. Create the migration file `supabase/migrations/20260224000000_phase1_data_foundation.sql` with the EXACT schema from 01-RESEARCH.md "Complete Migration File" section. This includes:

   a. `handle_updated_at()` trigger function (shared)

   b. `quiz_sessions` table with ALL locked columns from CONTEXT.md:
      - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
      - `user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`
      - `email TEXT` (nullable — per research open question 2, session created before email captured)
      - `status TEXT NOT NULL DEFAULT 'in_progress' CHECK (status IN ('in_progress', 'completed'))`
      - `answers JSONB` (not JSON — per research, JSONB for binary storage and indexing)
      - `dimension_scores JSONB` (separate column, not inside answers — per CONTEXT.md lock)
      - `archetype_id TEXT` (dedicated column — per CONTEXT.md lock)
      - `cultural_background TEXT` (dedicated column, NOT buried in answers JSONB — per CONTEXT.md lock, critical for Phase 5-6 personalization)
      - `started_at TIMESTAMPTZ NOT NULL DEFAULT now()`
      - `completed_at TIMESTAMPTZ`
      - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
      - `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`
      - RLS enabled immediately
      - Index on `user_id`
      - `updated_at` trigger attached

   c. quiz_sessions RLS policies (3 policies):
      - INSERT: `auth.uid() = user_id` (anon + authenticated)
      - SELECT: `auth.uid() = user_id` (anon + authenticated)
      - UPDATE: `auth.uid() = user_id` both USING and WITH CHECK (anon + authenticated)

   d. `orders` table with ALL locked columns from CONTEXT.md:
      - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
      - `quiz_session_id UUID NOT NULL REFERENCES quiz_sessions(id) ON DELETE RESTRICT`
      - `user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE RESTRICT`
      - `stripe_checkout_session_id TEXT UNIQUE NOT NULL` (idempotency guard — per CONTEXT.md lock)
      - `status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'paid', 'generating', 'fulfilled', 'delivered'))` (5-status lifecycle — per CONTEXT.md lock)
      - `amount_cents INTEGER NOT NULL` (per CONTEXT.md lock — supports future pricing changes)
      - `customer_email TEXT NOT NULL`
      - `pdf_storage_path TEXT`
      - `paid_at TIMESTAMPTZ`
      - `fulfilled_at TIMESTAMPTZ`
      - `delivered_at TIMESTAMPTZ`
      - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
      - `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`
      - RLS enabled immediately
      - Indexes on `user_id`, `quiz_session_id`, `stripe_checkout_session_id`
      - `updated_at` trigger attached

   e. orders RLS policy (1 policy):
      - SELECT only: `auth.uid() = user_id` (anon + authenticated)
      - No INSERT/UPDATE for anon — orders created server-side via service_role (Stripe webhook)

   f. Storage bucket creation:
      - `INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types) VALUES ('blueprints', 'blueprints', false, 52428800, ARRAY['application/pdf'])`
      - Storage RLS: service_role can INSERT, anon/authenticated can SELECT (bucket_id = 'blueprints')

4. Create `.env.local.example` with placeholder variables:
   ```
   NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
   ```

CRITICAL: Do NOT deviate from the schema in 01-RESEARCH.md. Every column, constraint, and policy is derived from locked CONTEXT.md decisions. Approximations cause Phase 2-6 rework.
  </action>
  <verify>
    <automated>npx supabase db lint --level warning supabase/migrations/20260224000000_phase1_data_foundation.sql 2>&1 || echo "Lint not available — verify SQL syntax manually"; grep -c "CREATE TABLE" supabase/migrations/20260224000000_phase1_data_foundation.sql | xargs -I{} test {} -eq 2 && echo "PASS: 2 tables found" || echo "FAIL: expected 2 CREATE TABLE statements"; grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260224000000_phase1_data_foundation.sql | xargs -I{} test {} -eq 2 && echo "PASS: RLS enabled on 2 tables" || echo "FAIL: expected 2 ENABLE ROW LEVEL SECURITY"; grep "enable_anonymous_sign_ins = true" supabase/config.toml && echo "PASS: anonymous auth enabled" || echo "FAIL: anonymous auth not enabled in config.toml"</automated>
    <manual>Review migration SQL against 01-RESEARCH.md complete migration file to confirm all columns, constraints, and policies match</manual>
  </verify>
  <done>
    - supabase/ directory initialized with config.toml having anonymous auth enabled
    - Migration SQL file contains quiz_sessions and orders tables with all locked columns
    - RLS enabled on both tables with correct policies (3 for quiz_sessions, 1 for orders)
    - Indexes on user_id, quiz_session_id, stripe_checkout_session_id
    - updated_at triggers on both tables
    - blueprints private storage bucket with PDF-only mime type
    - .env.local.example with all required environment variable placeholders
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Supabase dependencies and create client helper files</name>
  <files>
    package.json
    lib/supabase/client.ts
    lib/supabase/server.ts
  </files>
  <action>
1. Install Supabase dependencies:
   ```bash
   npm install @supabase/supabase-js @supabase/ssr
   ```

2. Create `lib/supabase/client.ts` — browser client (anon key, safe for client-side):
   ```typescript
   import { createBrowserClient } from '@supabase/ssr'

   export function createClient() {
     return createBrowserClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
     )
   }
   ```

   This uses `@supabase/ssr` (not deprecated `auth-helpers`) per research "State of the Art" section.

3. Create `lib/supabase/server.ts` — server-only admin client (service_role key, bypasses RLS):
   ```typescript
   // WARNING: service_role key bypasses ALL Row Level Security policies.
   // This client is for server-side operations ONLY (e.g., Stripe webhook handler).
   // NEVER import this file in client components, pages, or any browser-executed code.
   // The SUPABASE_SERVICE_ROLE_KEY env var must NOT have the NEXT_PUBLIC_ prefix.
   import { createClient } from '@supabase/supabase-js'

   export function createAdminClient() {
     return createClient(
       process.env.SUPABASE_URL ?? process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.SUPABASE_SERVICE_ROLE_KEY!,
     )
   }
   ```

   Note: `createAdminClient` (not `createClient`) to make the dangerous nature obvious at import sites. The server URL uses SUPABASE_URL (without NEXT_PUBLIC_ prefix) as primary, falling back to the public URL for local dev simplicity.

CRITICAL per research Pitfall 3: The service_role key must NEVER appear in client-side code. The `SUPABASE_SERVICE_ROLE_KEY` env var deliberately has no `NEXT_PUBLIC_` prefix — Next.js will not bundle it into the client.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); const deps = pkg.dependencies || {}; if (deps['@supabase/supabase-js'] && deps['@supabase/ssr']) { console.log('PASS: Supabase packages installed') } else { console.error('FAIL: missing Supabase packages'); process.exit(1) }"; npx tsc --noEmit lib/supabase/client.ts lib/supabase/server.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - @supabase/supabase-js and @supabase/ssr installed in package.json
    - lib/supabase/client.ts exports createClient using createBrowserClient from @supabase/ssr
    - lib/supabase/server.ts exports createAdminClient using createClient from @supabase/supabase-js with service_role key
    - No TypeScript errors in either file
    - Service role key is NOT exposed via NEXT_PUBLIC_ prefix
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `supabase/config.toml` exists with `enable_anonymous_sign_ins = true`
2. Migration SQL file contains exactly 2 CREATE TABLE statements with RLS enabled
3. All locked CONTEXT.md columns present in migration: cultural_background (dedicated TEXT column), dimension_scores (dedicated JSONB column), archetype_id (dedicated TEXT column), stripe_checkout_session_id (UNIQUE NOT NULL), amount_cents (INTEGER NOT NULL), 5-status CHECK constraint on orders
4. @supabase/supabase-js and @supabase/ssr in package.json dependencies
5. lib/supabase/client.ts uses createBrowserClient (not deprecated auth-helpers)
6. lib/supabase/server.ts uses service_role key with no NEXT_PUBLIC_ prefix
</verification>

<success_criteria>
- Supabase project directory fully initialized and ready for `supabase start`
- Migration SQL implements the exact schema contract from CONTEXT.md and RESEARCH.md
- Client helpers ready for downstream phases to import
- No schema approximations — every locked decision implemented precisely
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
