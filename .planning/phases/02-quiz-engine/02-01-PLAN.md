---
phase: 02-quiz-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - next.config.ts
  - package.json
  - tsconfig.json
  - biome.json
  - components/ui/
autonomous: true
requirements:
  - QUIZ-04

must_haves:
  truths:
    - "Next.js dev server starts and renders a page at localhost:3000"
    - "Tailwind CSS utility classes produce visible styling"
    - "shadcn/ui Button component renders correctly"
    - "Biome lints and formats TypeScript files"
    - "Existing lib/ files (archetypes, quiz, supabase) are importable with @/ alias"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with html, body, font setup"
      min_lines: 10
    - path: "app/page.tsx"
      provides: "Minimal placeholder page with Start Quiz CTA linking to /quiz"
      min_lines: 5
    - path: "app/globals.css"
      provides: "Tailwind CSS v4 base styles and theme tokens"
      min_lines: 5
    - path: "next.config.ts"
      provides: "Next.js configuration"
      min_lines: 3
    - path: "biome.json"
      provides: "Biome linter/formatter configuration"
      min_lines: 5
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "tsconfig.json"
      to: "lib/"
      via: "@/* path alias"
      pattern: "@/\\*.*\\./\\*"
---

<objective>
Bootstrap the Next.js application scaffold with all Phase 2 dependencies installed and configured.

Purpose: The project currently has no Next.js application structure (only lib/, scripts/, and supabase/ directories). Every subsequent plan depends on a working Next.js dev server with Tailwind CSS, shadcn/ui, and Biome configured.

Output: A running `npm run dev` that serves a minimal page at localhost:3000, with all existing lib/ code accessible via @/ imports.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quiz-engine/02-RESEARCH.md
@package.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js project and install all Phase 2 dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    app/layout.tsx
    app/page.tsx
    app/globals.css
  </files>
  <action>
    Bootstrap Next.js into the existing project. The project already has lib/, scripts/, supabase/, package.json, and tsconfig.json — these MUST be preserved.

    **Step 1: Run create-next-app in a temp directory, then merge:**
    ```bash
    npx create-next-app@latest /tmp/rg-bootstrap --typescript --tailwind --app --no-eslint --no-src-dir --yes
    ```
    Copy from /tmp/rg-bootstrap into the project:
    - `app/` directory (layout.tsx, page.tsx, globals.css)
    - `next.config.ts`
    - `public/` directory (favicon, etc.)
    Do NOT overwrite existing package.json or tsconfig.json — merge them.

    **Step 2: Merge package.json:**
    Keep existing dependencies (@supabase/ssr, @supabase/supabase-js, tsx). Add Next.js dependencies (next, react, react-dom). Add quiz dependencies:
    ```bash
    npm install next react react-dom zustand nuqs react-hook-form zod @hookform/resolvers
    npm install --save-dev @biomejs/biome @types/react @types/react-dom @types/node
    ```

    **Step 3: Merge tsconfig.json:**
    Update tsconfig.json to work with Next.js while preserving the existing @/* path alias. The Next.js tsconfig needs:
    - `"jsx": "preserve"`
    - `"lib": ["dom", "dom.iterable", "esnext"]`
    - `"plugins": [{ "name": "next" }]`
    - `"paths": { "@/*": ["./*"] }` (already exists)
    - `"include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]`

    **Step 4: Configure next.config.ts:**
    ```typescript
    import type { NextConfig } from 'next'
    const nextConfig: NextConfig = {
      serverExternalPackages: ['@react-pdf/renderer'],  // Phase 6 prep
    }
    export default nextConfig
    ```

    **Step 5: Update app/layout.tsx:**
    Set up root layout with a warm, clean font (system font stack or Inter via next/font/google). Import globals.css. Set page title metadata to "Your Parenting Blueprint".

    **Step 6: Update app/page.tsx:**
    Create a minimal placeholder page — centered "Take the Quiz" button that links to /quiz. This is NOT the Phase 7 landing page — just a development entry point so testers can access the quiz.

    **Step 7: Add dev/build scripts to package.json:**
    ```json
    "scripts": {
      "dev": "next dev",
      "build": "next build",
      "start": "next start",
      "lint": "biome check .",
      "format": "biome format . --write"
    }
    ```
    Keep existing scripts (validate-scoring, verify-rls).
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npm run build 2>&1 | tail -5</automated>
    <manual>Run `npm run dev` and visit http://localhost:3000 — page renders with "Take the Quiz" button</manual>
  </verify>
  <done>
    `npm run dev` starts the Next.js dev server. localhost:3000 renders a page with a "Take the Quiz" link to /quiz. Existing lib/ code is importable via @/ alias. package.json has all Phase 2 dependencies. tsconfig.json works for both Next.js and existing tsx scripts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and Biome configuration</name>
  <files>
    biome.json
    components/ui/button.tsx
    components/ui/input.tsx
    components/ui/progress.tsx
    lib/utils.ts
  </files>
  <action>
    **Step 1: Initialize shadcn/ui:**
    ```bash
    npx shadcn@latest init --defaults
    ```
    This creates `components/ui/` directory and `lib/utils.ts` (cn utility function using clsx + tailwind-merge). If the CLI prompts for Tailwind CSS version, select v4 (CSS-based config, no tailwind.config.js).

    **Step 2: Add required shadcn/ui components:**
    ```bash
    npx shadcn@latest add button input progress
    ```
    These are the primitives needed for the quiz: Button (navigation, CTA), Input (email capture), Progress (progress bar base).

    **Step 3: Configure Biome:**
    Create `biome.json` with:
    ```json
    {
      "$schema": "https://biomejs.dev/schemas/2.0.0/schema.json",
      "organizeImports": { "enabled": true },
      "linter": {
        "enabled": true,
        "rules": {
          "recommended": true,
          "correctness": {
            "useExhaustiveDependencies": "warn"
          }
        }
      },
      "formatter": {
        "enabled": true,
        "indentStyle": "space",
        "indentWidth": 2,
        "lineWidth": 100
      },
      "files": {
        "ignore": [".next", "node_modules", "public"]
      }
    }
    ```

    **Step 4: Verify Biome runs:**
    ```bash
    npx biome check .
    ```
    Fix any auto-fixable issues. This ensures clean baseline before quiz code is written.

    **Step 5: Add .gitignore entries** for Next.js if not already present:
    ```
    .next/
    out/
    next-env.d.ts
    ```
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx biome check app/ components/ --no-errors-on-unmatched 2>&1 | tail -3</automated>
    <manual>Confirm components/ui/button.tsx exists and exports a Button component</manual>
  </verify>
  <done>
    shadcn/ui initialized with Button, Input, and Progress components in components/ui/. Biome configured and runs without errors on the project. lib/utils.ts provides the cn() utility function. .gitignore includes .next/ and node_modules/.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- `npm run build` completes successfully
- `npx biome check .` runs without errors
- `components/ui/button.tsx` exists and exports a Button component
- Existing `lib/quiz/dimensions.ts` is importable from app/ code using `@/lib/quiz/dimensions`
- `app/page.tsx` renders at localhost:3000 with a link to /quiz
</verification>

<success_criteria>
The Next.js application scaffold is complete with all Phase 2 dependencies installed. The dev server runs, Tailwind CSS works, shadcn/ui components are available, Biome is configured, and all existing lib/ code remains functional and importable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quiz-engine/02-01-SUMMARY.md`
</output>
