---
phase: 05-landing-page
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - components/landing/ProductLadder.tsx
  - components/landing/SophiaSection.tsx
  - components/landing/FAQSection.tsx
  - components/landing/FinalCTASection.tsx
  - app/page.tsx
autonomous: false
requirements:
  - LAND-01
  - LAND-03
  - LAND-04
  - LAND-05
  - BRAND-06

must_haves:
  truths:
    - "Product ladder shows three cards (Mirror free/live, Blueprint coming soon, Partner Match future) with correct status badges"
    - "Sophia section displays clinical credentials and personal connection to the work"
    - "FAQ section has 5-7 expandable questions using HTML details/summary (zero JS)"
    - "Final CTA section has emotional callback copy and a Link-wrapped quiz button"
    - "Complete landing page renders all 9+ sections in the correct emotional arc order"
    - "Every 'Take The Mirror' CTA on the page navigates to /quiz"
  artifacts:
    - path: "components/landing/ProductLadder.tsx"
      provides: "Three product cards (Mirror, Blueprint, Partner Match) with status badges"
      min_lines: 50
    - path: "components/landing/SophiaSection.tsx"
      provides: "Clinical co-founder credentials section with placeholder for photo"
      min_lines: 40
    - path: "components/landing/FAQSection.tsx"
      provides: "5-7 FAQ items using HTML details/summary accordion"
      min_lines: 50
    - path: "components/landing/FinalCTASection.tsx"
      provides: "Emotional callback CTA with Link to /quiz"
      min_lines: 30
    - path: "app/page.tsx"
      provides: "Complete page shell with all sections imported and rendered"
      contains: "FinalCTASection"
  key_links:
    - from: "components/landing/ProductLadder.tsx"
      to: "/quiz"
      via: "Link-wrapped CTA on Mirror card"
      pattern: "Link.*href.*quiz"
    - from: "components/landing/FinalCTASection.tsx"
      to: "/quiz"
      via: "Link-wrapped CTA button"
      pattern: "Link.*href.*quiz"
    - from: "components/landing/FAQSection.tsx"
      to: "HTML details/summary"
      via: "Native HTML accordion (zero JS)"
      pattern: "<details"
    - from: "app/page.tsx"
      to: "components/landing/*"
      via: "All section imports assembled"
      pattern: "import.*FinalCTASection"
---

<objective>
Build the bottom-of-page sections that deliver the "Action" phase of the landing page emotional arc: product ladder (what you get), Sophia credentials (who built it), FAQ (last objections), and final CTA (the conversion moment). Assemble the complete page and verify with human review.

Purpose: Completes the full landing page conversion funnel. The product ladder shows the vision. Sophia grounds it in clinical authority (LAND-05). FAQ removes last-moment friction. Final CTA closes the loop with emotional callback to the hero headline.

Output: Complete, fully-functional landing page at localhost:3000 with all 9+ sections, all CTAs routing to /quiz, mobile-responsive, and ready for human verification.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-landing-page/VISUAL-DIRECTION.md
@.planning/phases/05-landing-page/05-CONTEXT.md
@.planning/phases/05-landing-page/05-RESEARCH.md
@.planning/phases/05-landing-page/05-01-SUMMARY.md
@.planning/phases/05-landing-page/05-02-SUMMARY.md
@app/page.tsx
@lib/landing/palette.ts

<interfaces>
<!-- From Plan 01 — shared constants -->
From lib/landing/palette.ts:
```typescript
export const ff: string;       // Plus Jakarta Sans font family
export const ffSerif: string;  // Instrument Serif font family
export const ffDisplay: string; // Space Grotesk font family
export const p: {
  blue:   { light: "#B3D5DE", dark: "#002833" },
  pink:   { light: "#EEC0DA", dark: "#4A1942" },
  mint:   { light: "#B2DECD", dark: "#1A4A3A" },
  butter: { light: "#FEF4AC", dark: "#3D3B1A" },
};
```

<!-- Product ladder content from CONTEXT.md decisions -->
Products:
- Mirror: "Free — Available now" + "Take The Mirror" CTA. "Understand how the parenting you received shapes who you are today."
- Blueprint: "Coming soon". "A personalized guide to understanding and reshaping your patterns."
- Partner Match: "Future". "See how your parenting patterns interact with your partner's."

<!-- FAQ from CONTEXT.md (Claude's discretion on wording) -->
Suggested topics:
1. Is this real science?
2. How long does it take?
3. Is it really free?
4. What happens with my email?
5. Who made this?
6. What will I learn?
7. Can I retake the quiz?

<!-- Sophia section — no photo exists, design for copy-forward layout -->
Credentials to author: clinical psychologist co-founder, specialization in attachment theory and family systems, personal connection to the work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProductLadder and SophiaSection components</name>
  <files>
    components/landing/ProductLadder.tsx
    components/landing/SophiaSection.tsx
  </files>
  <action>
**1. Create `components/landing/ProductLadder.tsx` (Server Component, no "use client"):**

Import `{ ff, ffSerif, p }` from `@/lib/landing/palette`. Import `Link` from `next/link`.

Per CONTEXT.md decisions:
- Three side-by-side cards with status badges (grid grid-cols-1 lg:grid-cols-3 gap-4)
- Section label: "The product" or "What you get" (uppercase tracking)
- Heading: "Three tools for *understanding yourself*" (or similar — Claude's discretion)

Card specifications:
- **Mirror card**: Most prominent. Solid butter or blue light bg, 24px radius.
  - Status badge: "Free — Available now" — green/mint accent pill
  - Title: "The Mirror" (ff 800)
  - Description: "Understand how the parenting you received shapes who you are today."
  - CTA: "Take The Mirror" button wrapped in `<Link href="/quiz">` — dark teal bg, 16px radius, min-height 44px
- **Blueprint card**: Muted. Lighter styling to indicate coming-soon.
  - Status badge: "Coming soon" — subtle pill, muted color
  - Title: "The Blueprint" (ff 700)
  - Description: "A personalized guide to understanding and reshaping your patterns."
  - NO CTA button, NO email waitlist (per CONTEXT.md — no competing CTAs)
- **Partner Match card**: Most muted. Future state.
  - Status badge: "Future" — subtle pill, most muted color
  - Title: "The Partner Match" (ff 700)
  - Description: "See how your parenting patterns interact with your partner's."
  - NO CTA button

Layout: Section padding py-20 md:py-28 px-4 md:px-10, max-w-7xl mx-auto. Cards should feel like a progression — Mirror is clearly the one to engage with now.

Use content-based keys (product name or title).

**2. Create `components/landing/SophiaSection.tsx` (Server Component, no "use client"):**

Import `{ ff, ffSerif, p }` from `@/lib/landing/palette`.

Per CONTEXT.md: Separate from KOL section, elevated, personal. Positioned after KOL credibility.

**IMPORTANT: No photo asset exists.** Design the section copy-forward — it must look complete and credible without a photo. Options:
- Use a styled initial/monogram in a pastel circle (e.g., "S" in mint or pink bg, using ff 800)
- OR skip the image entirely and use a large serif-italic quote from Sophia as the visual anchor

Section content (Claude authors this copy):
- Section label: "The creator" or "Who built this" (uppercase tracking)
- Heading: "Built by someone who *gets it*" (or similar — Claude's discretion)
- Sophia's credentials:
  - Clinical psychologist
  - Specialization in attachment theory and family systems
  - Personal connection: she built this because she experienced the patterns herself (or similar warm, human motivation)
- Styled as a two-column layout on desktop (text left, styled initial/credentials right) or a centered editorial block
- Warm tone — this is personal, not a resume. Think "a therapist friend explaining why she made this."
- Background: could use a subtle pastel card (pink light bg) or stay on warm base
- Section padding: py-20 md:py-28 px-4 md:px-10, max-w-7xl mx-auto
- Add a `{/* TODO: Replace styled initial with real photo when available */}` comment where the photo placeholder goes

**Anti-patterns to avoid:**
- Do NOT add email capture to Blueprint/Partner Match cards (CONTEXT.md: no competing CTAs)
- Do NOT use `<img>` or `<Image>` for Sophia without verifying the file exists — no photo in /public/
- Do NOT use emoji icons
- No glassmorphic cards
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ProductLadder renders 3 product cards with correct status badges and hierarchy
    - Mirror card has Link-wrapped CTA to /quiz
    - Blueprint and Partner Match cards have NO CTAs and NO email capture
    - SophiaSection renders clinical credentials with copy-forward layout (no broken image)
    - Both are Server Components
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FAQSection and FinalCTASection, assemble complete page</name>
  <files>
    components/landing/FAQSection.tsx
    components/landing/FinalCTASection.tsx
    app/page.tsx
  </files>
  <action>
**1. Create `components/landing/FAQSection.tsx` (Server Component, no "use client"):**

Import `{ ff, p }` from `@/lib/landing/palette`.

Use HTML `<details>/<summary>` for zero-JS accordion — consistent with WatchoutsSection.tsx pattern in the result page.

- Section label: "Questions" or "FAQ" (uppercase tracking)
- Heading: "Before you start" (or similar — Claude's discretion)
- 5-7 FAQ items. Claude writes the questions and answers. Suggested topics from CONTEXT.md:

```typescript
const FAQS = [
  {
    q: "Is this based on real science?",
    a: "Yes. Every question maps to one of 11 research-backed dimensions drawn from the work of John Gottman, Daniel Siegel, Mary Ainsworth, and other leading attachment and parenting researchers. The framework was developed in collaboration with a clinical psychologist specializing in family systems."
  },
  {
    q: "How long does it take?",
    a: "About 15 minutes. There are 21 thoughtful questions about the parenting you received growing up. There's no timer — take as long as you need."
  },
  {
    q: "Is it really free?",
    a: "Completely free. The Mirror is our core product — you'll see your full result with no paywall, no blurred content, and no hidden charges. We believe everyone deserves to understand themselves."
  },
  {
    q: "What happens with my email?",
    a: "We ask for your email before showing your result so we can send you a copy. We'll never sell your data or spam you. You may occasionally hear from us about new tools — you can unsubscribe anytime."
  },
  {
    q: "Who built this?",
    a: "Kin was created by a team that includes a clinical psychologist specializing in attachment theory and family systems. The framework draws on decades of peer-reviewed research to help you understand the parenting you received."
  },
  {
    q: "What will I learn?",
    a: "You'll discover which of 9 parenting archetypes shaped your upbringing, along with the specific patterns, strengths, and watchouts that come with it — all personalized to your cultural background."
  },
  {
    q: "Can I retake the quiz?",
    a: "The Mirror is designed as a one-time reflection. Your result is based on your genuine first responses, and retaking it tends to produce less authentic answers. Trust your initial instincts."
  },
];
```

Each FAQ item:
```html
<details className="group border-b border-[#002833]/10">
  <summary className="cursor-pointer py-5 list-none flex items-center justify-between" style={{ fontFamily: ff, fontWeight: 600 }}>
    <span>{faq.q}</span>
    <span className="group-open:rotate-180 transition-transform text-sm" style={{ color: "#999" }}>▾</span>
  </summary>
  <p className="pb-5 text-sm leading-relaxed" style={{ fontFamily: ff, fontWeight: 400, color: "#666" }}>
    {faq.a}
  </p>
</details>
```

Use `faq.q` as key (unique question strings).

Section padding: py-20 md:py-28 px-4 md:px-10, max-w-7xl mx-auto, max-w-3xl for the FAQ container itself (narrower for readability).

**2. Create `components/landing/FinalCTASection.tsx` (Server Component, no "use client"):**

Import `{ ff, ffSerif, p }` from `@/lib/landing/palette`. Import `Link` from `next/link`.

Per CONTEXT.md: "Emotional callback, not a repeat of the hero. Something like 'Ready to see what shaped you?'"

- Centered layout, generous padding (py-24 md:py-32)
- Large heading: "Ready to see what *shaped you*?" (shaped you in serif italic pink) — or similar emotional callback
- Subtext: Brief encouraging line (ff 400, color #888)
- Single CTA button: "Take The Mirror" wrapped in `<Link href="/quiz">` — dark teal bg, 16px radius, min-height 44px, generous padding (px-10 py-5)
- Background: could use a subtle pastel tint (butter light at low opacity) or a dark teal full-width band for contrast
- This is the conversion climax — should feel like the page has been building to this moment
- Below the CTA: small footer text "The Mirror by Kin" with tagline, matching the dark footer bar in the hero

**3. Assemble complete page in `app/page.tsx`:**

Add imports for ProductLadder, SophiaSection, FAQSection, FinalCTASection. Remove all Plan 03 placeholder comments. The final page shell should render:

```typescript
<LandingNav />
<HeroSection />
<EmotionalHookSection />
<HowItWorksProcess />
<HowItWorksScience />
<KOLSection />
<ProductLadder />
<SophiaSection />
<FAQSection />
<FinalCTASection />
```

Ensure page.tsx remains a Server Component (no "use client"). All sections should be in the correct emotional arc order: Recognition → Validation → Action.

**Anti-patterns to avoid:**
- Do NOT use JavaScript for FAQ accordion — `<details>/<summary>` only
- Do NOT use Radix UI Accordion
- Do NOT put multiple `"use client"` components — only KOLSection needs it
- Do NOT use array index keys — use faq.q or product name
- No emoji icons
- Touch targets on all CTAs: min-height 44px
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - FAQSection renders 5-7 expandable FAQ items using HTML details/summary
    - FinalCTASection renders emotional callback with Link-wrapped CTA to /quiz
    - app/page.tsx imports and renders all 10 section components in correct order
    - Page is a Server Component (no "use client" on page.tsx)
    - Build succeeds, lint passes
    - Complete landing page renders at localhost:3000
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete landing page</name>
  <files>app/page.tsx</files>
  <action>
Human verifies the complete landing page visually and functionally.

What was built: Complete landing page for "The Mirror by Kin" with 9+ sections implementing the locked visual direction (Warm Solid / Mid Blend ~35%), full emotional arc (Recognition → Validation → Action), 12 named researcher credibility signals, product ladder, Sophia credentials, FAQ, and functional CTAs routing to /quiz.

How to verify:
1. Start dev server: `npm run dev`
2. Visit http://localhost:3000

**Visual check:**
- Hero: Asymmetric 7/5 grid, butter hero card, mixed serif+sans headline, pastel pills, pink quote card, mint/blue stat cards, dark footer bar
- All text uses Plus Jakarta Sans / Instrument Serif / Space Grotesk (NOT Geist)
- Cards are solid pastel fills (not glassmorphic, not gradient borders)
- Nav has glassmorphic pill effect (the one modern touch)
- Background is warm (#FAFAF7) with subtle gradient wash at top

**Content check:**
- Emotional hook below hero bridges to "why"
- How It Works: 3 step cards + science stat cards
- KOL: 6 researchers visible, "See all" expands to 12
- Product ladder: Mirror (free, CTA), Blueprint (coming soon, no CTA), Partner Match (future, no CTA)
- Sophia: credentials, personal connection, no broken images
- FAQ: 5-7 expandable questions, each opens/closes independently
- Final CTA: emotional callback, not a repeat of hero headline

**Functional check:**
- Click "Take The Mirror" in hero → navigates to /quiz
- Click "Start free" in nav → navigates to /quiz
- Click "Take The Mirror" on Mirror product card → navigates to /quiz
- Click "Take The Mirror" in final CTA → navigates to /quiz
- "See all researchers" expands KOL section
- FAQ items expand/collapse

**Mobile check:**
- Resize browser to 375px width
- Hero stacks to single column
- All sections are readable on mobile
- Touch targets are adequately sized

Resume: Type "approved" if the landing page looks complete and functional, or describe specific issues to fix.
  </action>
  <verify>Human visual and functional verification of the complete landing page at localhost:3000</verify>
  <done>Human has approved the complete landing page or provided feedback for iteration</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `npm run lint` passes
3. Complete landing page at localhost:3000 renders all sections in order:
   Nav → Hero → Emotional Hook → How It Works (Process) → How It Works (Science) → KOL → Product Ladder → Sophia → FAQ → Final CTA
4. All "Take The Mirror" CTAs navigate to /quiz (4 CTA locations)
5. KOL expand/collapse works
6. FAQ details/summary accordion works
7. Mobile responsive at 375px, 768px, 1024px widths
8. No broken images, no console errors
9. Human approval received
</verification>

<success_criteria>
- LAND-01: Clear value proposition through hero + emotional hook + how-it-works sections
- LAND-02: 12 named researchers with research attribution in KOL section (8+ visible)
- LAND-03: All CTAs route to /quiz (4 locations: nav, hero, product ladder, final CTA)
- LAND-04: Full mobile responsiveness across all sections
- LAND-05: Sophia credentials visible in dedicated section
- BRAND-06: Color palette correctly implemented throughout (4 pastel/dark pairs)
- BRAND-07: Typography system (Plus Jakarta Sans + Instrument Serif + Space Grotesk) across all sections
- BRAND-08: Rebrand execution complete — layout.tsx uses brand font, entire landing page follows visual direction
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-landing-page/05-03-SUMMARY.md`
</output>
