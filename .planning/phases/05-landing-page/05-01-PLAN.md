---
phase: 05-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/landing/palette.ts
  - app/layout.tsx
  - app/globals.css
  - components/landing/LandingNav.tsx
  - components/landing/HeroSection.tsx
  - components/landing/EmotionalHookSection.tsx
  - app/page.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-03
  - BRAND-06
  - BRAND-07
  - BRAND-08

must_haves:
  truths:
    - "Landing page renders at localhost:3000 with the locked visual direction (warm solid palette, correct fonts, asymmetric hero)"
    - "Clicking 'Take The Mirror' CTA in hero or nav routes to /quiz"
    - "Plus Jakarta Sans is the body font across the entire app (not Geist)"
    - "Emotional hook section appears below the hero with recognition-phase copy"
    - "Page is mobile-responsive -- hero stacks to single column on mobile"
  artifacts:
    - path: "lib/landing/palette.ts"
      provides: "Shared font family strings and palette constants"
      exports: ["ff", "ffSerif", "ffDisplay", "p"]
    - path: "components/landing/HeroSection.tsx"
      provides: "Asymmetric 7/5 hero with butter card, pills, headline, Link-wrapped CTAs"
      min_lines: 80
    - path: "components/landing/LandingNav.tsx"
      provides: "Glassmorphic pill nav with Kin brand, segmented toggle, Link-wrapped CTA"
      min_lines: 30
    - path: "components/landing/EmotionalHookSection.tsx"
      provides: "Below-fold emotional bridge section (recognition phase of arc)"
      min_lines: 20
    - path: "app/page.tsx"
      provides: "Thin shell importing section components, no 'use client'"
      contains: "import.*HeroSection"
    - path: "app/layout.tsx"
      provides: "Root layout with Plus Jakarta Sans as body font"
      contains: "Plus_Jakarta_Sans"
  key_links:
    - from: "components/landing/HeroSection.tsx"
      to: "/quiz"
      via: "next/link Link component wrapping CTA button"
      pattern: "Link.*href.*quiz"
    - from: "components/landing/LandingNav.tsx"
      to: "/quiz"
      via: "next/link Link component wrapping nav CTA"
      pattern: "Link.*href.*quiz"
    - from: "app/page.tsx"
      to: "components/landing/*"
      via: "import statements for section components"
      pattern: "import.*components/landing"
    - from: "lib/landing/palette.ts"
      to: "components/landing/*"
      via: "shared font and color constants imported by all sections"
      pattern: "import.*lib/landing/palette"
---

<objective>
Extract shared design constants, update the app's font foundation from Geist to Plus Jakarta Sans, decompose the existing hero prototype into maintainable section components, add the emotional hook section, and wire everything through a thin page.tsx shell with Link-wrapped CTAs that route to /quiz.

Purpose: Establishes the design system foundation (palette, typography) and the page shell architecture that all subsequent plans build upon. Without this, downstream section components have no shared constants and no page to attach to.

Output: Working landing page at localhost:3000 with hero + emotional hook, correct brand fonts, functional CTAs routing to /quiz, and placeholder comments for remaining sections.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-landing-page/VISUAL-DIRECTION.md
@.planning/phases/05-landing-page/05-CONTEXT.md
@.planning/phases/05-landing-page/05-RESEARCH.md
@app/page.tsx
@app/layout.tsx
@app/globals.css

<interfaces>
<!-- The existing page.tsx contains the full hero prototype with all visual patterns. -->
<!-- Extract these patterns into reusable constants and components. -->

From app/page.tsx (current pattern to extract):
```typescript
const p = {
  blue: { light: "#B3D5DE", dark: "#002833" },
  pink: { light: "#EEC0DA", dark: "#4A1942" },
  mint: { light: "#B2DECD", dark: "#1A4A3A" },
  butter: { light: "#FEF4AC", dark: "#3D3B1A" },
};
const ff = plusJakarta.style.fontFamily;
const ffSerif = instrumentSerif.style.fontFamily;
const ffDisplay = spaceGrotesk.style.fontFamily;
```

From app/layout.tsx (current — to be replaced):
```typescript
import { Geist } from "next/font/google";
const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
// body className: `${geistSans.variable} antialiased`
```

From app/globals.css (font-sans reference to update):
```css
--font-sans: var(--font-geist-sans);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared palette constants and update app font foundation</name>
  <files>
    lib/landing/palette.ts
    app/layout.tsx
    app/globals.css
  </files>
  <action>
**1. Create `lib/landing/palette.ts`:**

Initialize the three Google Fonts at module level using `next/font/google` — these are safe to import in any file (Next.js deduplicates by URL):

```typescript
import { Space_Grotesk, Instrument_Serif, Plus_Jakarta_Sans } from "next/font/google";

export const spaceGrotesk = Space_Grotesk({ subsets: ["latin"], weight: ["400","500","600","700"] });
export const instrumentSerif = Instrument_Serif({ subsets: ["latin"], weight: ["400"], style: ["normal","italic"] });
export const plusJakarta = Plus_Jakarta_Sans({ subsets: ["latin"], weight: ["400","500","600","700","800"] });

export const ff = plusJakarta.style.fontFamily;
export const ffSerif = instrumentSerif.style.fontFamily;
export const ffDisplay = spaceGrotesk.style.fontFamily;

export const p = {
  blue:   { light: "#B3D5DE", dark: "#002833" },
  pink:   { light: "#EEC0DA", dark: "#4A1942" },
  mint:   { light: "#B2DECD", dark: "#1A4A3A" },
  butter: { light: "#FEF4AC", dark: "#3D3B1A" },
};
```

**2. Update `app/layout.tsx`:**

- Replace `Geist` import with `Plus_Jakarta_Sans` from `next/font/google`
- Initialize: `const plusJakarta = Plus_Jakarta_Sans({ variable: "--font-plus-jakarta", subsets: ["latin"], weight: ["400","500","600","700","800"] });`
- Update body className: `${plusJakarta.variable} antialiased`
- Keep metadata exactly as-is (title: "Kin", description unchanged)

**3. Update `app/globals.css`:**

- Change `--font-sans: var(--font-geist-sans);` to `--font-sans: var(--font-plus-jakarta);`
- Leave all other CSS unchanged

**Important:** Do NOT add `"use client"` to palette.ts — it's a module-level constants file. Font objects are initialized at build time by Next.js.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - lib/landing/palette.ts exists and exports ff, ffSerif, ffDisplay, p
    - app/layout.tsx loads Plus Jakarta Sans (not Geist)
    - app/globals.css references --font-plus-jakarta (not --font-geist-sans)
    - Build succeeds with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Decompose hero into section components and create page shell</name>
  <files>
    components/landing/LandingNav.tsx
    components/landing/HeroSection.tsx
    components/landing/EmotionalHookSection.tsx
    app/page.tsx
  </files>
  <action>
**1. Create `components/landing/LandingNav.tsx` (Server Component, no "use client"):**

Extract the nav from current page.tsx. Import `{ ff, p }` from `@/lib/landing/palette`. Import `Link` from `next/link`.

Key changes from prototype:
- Wrap "Start free" button in `<Link href="/quiz">` — this is CRITICAL (current prototype has unrouted button)
- Add `minHeight: "44px"` to the CTA button for touch target compliance
- Keep glassmorphic pill nav styling exactly as prototype (backdrop-filter: blur(12px), rgba white bg, segmented toggle)
- The nav items ["The Mirror", "About"] can be anchor links or just visual for now — "About" could smooth-scroll to Sophia section later

**2. Create `components/landing/HeroSection.tsx` (Server Component, no "use client"):**

Extract the entire hero section from current page.tsx. Import `{ ff, ffSerif, p }` from `@/lib/landing/palette`. Import `Link` from `next/link`.

Key changes from prototype:
- Wrap "Take The Mirror" primary CTA in `<Link href="/quiz">` — CRITICAL
- Wrap "How it works" secondary CTA in `<Link href="#how-it-works">` (smooth scroll anchor)
- Add `minHeight: "44px"` to both CTA buttons for touch target compliance
- Keep the entire asymmetric 7/5 grid, butter card, pills, mixed serif+sans headline, quote card, stat cards, and footer bar exactly as prototype
- The gradient wash div should be part of the page shell or this component (keep it here since it's hero-specific)

**3. Create `components/landing/EmotionalHookSection.tsx` (Server Component, no "use client"):**

NEW section — does not exist in prototype. Import `{ ff, ffSerif, p }` from `@/lib/landing/palette`.

Content direction (Claude's discretion per CONTEXT.md):
- Full-width editorial block on warm background (#FAFAF7 — same as page bg, so it blends)
- Large serif-italic pull quote as the emotional anchor, something like: "The patterns from your childhood don't disappear. They just become invisible."
- Supporting body copy in Plus Jakarta Sans 400, color #888, max-w-lg
- Bridges the arc from "Finally see what shaped you" (hero) to "here's how" (How It Works)
- Section padding: py-20 md:py-28 px-4 md:px-10, max-w-7xl mx-auto
- No card — text on background. This is a breathing moment between hero and content sections.
- The pull quote should use ffSerif italic in a warm color (p.pink.dark or p.butter.dark)

**4. Rewrite `app/page.tsx` as thin page shell:**

- Remove `"use client"` directive — page.tsx should be a Server Component
- Remove all inline font imports, palette constants, researchers array, useState
- Import section components from `@/components/landing/`
- Import LandingNav, HeroSection, EmotionalHookSection
- Structure:

```typescript
import { LandingNav } from "@/components/landing/LandingNav";
import { HeroSection } from "@/components/landing/HeroSection";
import { EmotionalHookSection } from "@/components/landing/EmotionalHookSection";

export default function Home() {
  return (
    <div className="min-h-screen flex flex-col relative overflow-hidden" style={{ backgroundColor: "#FAFAF7", color: "#1A1A1A" }}>
      {/* Subtle gradient wash */}
      <div className="absolute top-0 left-0 right-0 h-[500px] pointer-events-none" style={{ background: "radial-gradient(ellipse 70% 50% at 60% 0%, rgba(254,244,172,0.18) 0%, transparent 70%), radial-gradient(ellipse 50% 40% at 30% 5%, rgba(238,192,218,0.12) 0%, transparent 60%)" }} />

      <LandingNav />
      <HeroSection />
      <EmotionalHookSection />

      {/* Sections added in Plan 02: HowItWorksProcess, HowItWorksScience, KOLSection */}
      {/* Sections added in Plan 03: ProductLadder, SophiaSection, FAQSection, FinalCTASection */}
    </div>
  );
}
```

**Anti-patterns to avoid:**
- Do NOT use array index keys anywhere (Biome noArrayIndexKey) — use content-based keys (pill.label, item name, etc.)
- Do NOT use emoji icons — use SVG from Lucide if any icons needed
- Do NOT add gradient text to headline — "see what" must be solid plum (#4A1942)
- Do NOT make cards glassmorphic — solid pastel fills only (glass is nav-only)
- Do NOT use `<button>` without Link wrapper for CTAs — every "Take The Mirror" must route to /quiz

**Named exports:** Use `export function ComponentName()` (not default exports) for all section components — consistent with project patterns and allows cleaner imports.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - app/page.tsx is a Server Component (no "use client") importing section components
    - LandingNav renders glassmorphic pill nav with Link-wrapped CTA to /quiz
    - HeroSection renders complete asymmetric 7/5 hero with Link-wrapped CTA to /quiz
    - EmotionalHookSection renders emotional bridge copy below hero
    - All components import from lib/landing/palette.ts (no inline font/palette definitions)
    - Build succeeds, no lint errors
    - Page renders at localhost:3000 with hero + emotional hook visible
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `npm run lint` passes (Biome check)
3. Landing page at localhost:3000 shows:
   - Glassmorphic nav with "Kin" brand and "Start free" CTA
   - Full hero with asymmetric grid, butter card, mixed serif+sans headline, pills, quote, stats
   - Emotional hook section below hero
4. Clicking "Take The Mirror" or "Start free" navigates to /quiz
5. Text on page uses Plus Jakarta Sans (not Geist)
6. Mobile responsive: hero stacks to single column at < lg breakpoint
</verification>

<success_criteria>
- Foundation established: shared palette.ts, brand fonts in layout, page shell architecture
- Hero decomposed: visual parity with existing prototype, CTAs now functional (Link to /quiz)
- Emotional hook added: recognition-phase bridge between hero and content sections
- No "use client" on page.tsx — Server Component shell
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-landing-page/05-01-SUMMARY.md`
</output>
