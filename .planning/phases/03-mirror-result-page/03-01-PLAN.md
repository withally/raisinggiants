---
phase: 03-mirror-result-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/result/page.tsx
  - components/result/ArchetypeReveal.tsx
  - components/result/FoundationalPatternsSection.tsx
  - components/result/WatchoutsSection.tsx
  - components/result/CulturalSection.tsx
  - lib/result/helpers.ts
autonomous: true
requirements: [RSLT-01, RSLT-03, RSLT-07, RSLT-08, RSLT-09]

must_haves:
  truths:
    - "Result page fetches quiz session from Supabase by session UUID and renders archetype content"
    - "Archetype name, tagline, and foundational patterns headline appear in a dramatic reveal section"
    - "All 5 foundational pattern themes are displayed in full with research anchor and citations"
    - "All 5 watchout themes are displayed in full with research anchor and citations"
    - "Cultural section renders conditionally based on user's cultural background selection"
    - "Cultural section displays expressionModifier, strengthsInContext, and tensionsInContext"
    - "Result page handles missing/invalid session gracefully with error state"
  artifacts:
    - path: "app/result/page.tsx"
      provides: "Server Component that fetches session data and passes to section components"
      exports: ["ResultPage (default)"]
    - path: "components/result/ArchetypeReveal.tsx"
      provides: "Hero reveal section with archetype name, tagline, headline"
      exports: ["ArchetypeReveal"]
    - path: "components/result/FoundationalPatternsSection.tsx"
      provides: "Full patterns section with all 5 themes and citations"
      exports: ["FoundationalPatternsSection"]
    - path: "components/result/WatchoutsSection.tsx"
      provides: "Full watchouts section with all 5 themes and citations"
      exports: ["WatchoutsSection"]
    - path: "components/result/CulturalSection.tsx"
      provides: "Conditional cultural lens section"
      exports: ["CulturalSection"]
    - path: "lib/result/helpers.ts"
      provides: "Cultural background lookup mapping and session fetch helper"
      exports: ["fetchQuizSession", "getCulturalOverlay", "CULTURAL_DISPLAY_NAMES"]
  key_links:
    - from: "app/result/page.tsx"
      to: "lib/supabase/server.ts"
      via: "createAdminClient() service_role fetch"
      pattern: "createAdminClient.*quiz_sessions.*select"
    - from: "app/result/page.tsx"
      to: "lib/archetypes/archetypes.ts"
      via: "ARCHETYPES array lookup by archetype_id"
      pattern: "ARCHETYPES\\.find.*id.*archetype_id"
    - from: "app/result/page.tsx"
      to: "lib/result/helpers.ts"
      via: "getCulturalOverlay maps cultural_background to CulturalOverlay"
      pattern: "getCulturalOverlay"
---

<objective>
Build the result page Server Component and all content section components. The result page fetches the quiz session from Supabase using the session UUID in the URL, looks up the matching archetype and cultural overlay, then renders: (1) archetype reveal hero, (2) foundational patterns, (3) watchouts, (4) cultural lens section.

Purpose: This is the emotional core of The Mirror — where the user feels deeply seen about the parenting they received. Every section must display complete content with no gating or blurring.
Output: Server Component result page + 4 section components + data helpers
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mirror-result-page/03-CONTEXT.md
@.planning/phases/03-mirror-result-page/03-RESEARCH.md
@lib/archetypes/types.ts
@lib/archetypes/archetypes.ts
@lib/archetypes/cultural-overlays.ts
@lib/supabase/server.ts
@app/result/page.tsx
@app/globals.css
@components/quiz/EmailCaptureScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create result data helpers and Server Component page shell</name>
  <files>lib/result/helpers.ts, app/result/page.tsx</files>
  <action>
Create `lib/result/helpers.ts` with:

1. `CULTURAL_DISPLAY_NAMES` — a Record mapping quiz option IDs to display section headers:
   - `"east-asian"` → `"The East Asian Lens"`
   - `"south-asian"` → `"The South Asian Lens"`
   - `"latin-american"` → `"The Latino/Hispanic Lens"`
   - `"sub-saharan"` → `"The Sub-Saharan African Lens"`
   - `"western"` → `"The Western Lens"`

2. `CULTURAL_CONTEXT_PREFIXES` — maps option IDs to the prefix of the `culturalContext` string used in cultural-overlays.ts constants:
   - `"east-asian"` → `"East Asian"`
   - `"south-asian"` → `"South Asian"`
   - `"latin-american"` → `"Latin American"`
   - `"sub-saharan"` → `"Sub-Saharan"`
   - `"western"` → `"Western"`

3. `getCulturalOverlay(archetype: Archetype, culturalBackground: string | null): CulturalOverlay | null` — if `culturalBackground` is null, `"other"`, or not in the prefix map, returns null. Otherwise finds the matching overlay from `archetype.culturalOverlays` array where `overlay.culturalContext.startsWith(prefix)`.

4. `fetchQuizSession(sessionId: string)` — uses `createAdminClient()` from `lib/supabase/server.ts` to fetch a single row from `quiz_sessions` by `id`, selecting `archetype_id, cultural_background, email, status`. Returns the row or null. Uses service_role to bypass RLS (the session UUID is the access credential).

Replace `app/result/page.tsx` (currently a placeholder) with the full async Server Component:
- Read `?session` from searchParams (Next.js 15+ pattern: `searchParams` is a Promise)
- If no session param, render an error state directing user to take the quiz
- Call `fetchQuizSession(session)` — if no row or status !== "completed", render error state
- Look up the `Archetype` from `ARCHETYPES` array using `archetype_id` (imported from `lib/archetypes/archetypes.ts`)
- Call `getCulturalOverlay(archetype, cultural_background)` to get the optional cultural overlay
- Get `CULTURAL_DISPLAY_NAMES[cultural_background]` for the section header
- Render the section components in order: `<ArchetypeReveal>`, `<FoundationalPatternsSection>`, `<WatchoutsSection>`, then conditionally `<CulturalSection>` if overlay exists
- Leave placeholder `{/* BlueprintCTA will be inserted here in Plan 02 */}` comments where the Blueprint CTA and sticky bar will go (between Watchouts and Cultural, and at page level)
- Keep existing `metadata` export for SEO
- Use `bg-amber-50` as the base page background (warm editorial palette)
- Error states: centered card on amber-50 background with display font headline, link to `/quiz`

Important patterns:
- Named exports for all components (`export function`)
- Display font applied via `style={{ fontFamily: "var(--font-display)" }}`
- Color palette: `bg-amber-50` base, alternating section backgrounds (`bg-white`, `bg-stone-50`), `text-stone-900` primary, `text-stone-600` secondary
- Biome-compatible: double quotes, semicolons, run `npx biome check --write` on all files
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit --pretty 2>&1 | head -30 && npx biome check lib/result/helpers.ts app/result/page.tsx 2>&1 | tail -5</automated>
    <manual>Verify that /result without session param shows error state, and /result?session=invalid shows error state</manual>
  </verify>
  <done>
    - `lib/result/helpers.ts` exports `fetchQuizSession`, `getCulturalOverlay`, `CULTURAL_DISPLAY_NAMES`, `CULTURAL_CONTEXT_PREFIXES`
    - `app/result/page.tsx` is an async Server Component that fetches session data and renders section components
    - Missing/invalid session shows a friendly error state directing to /quiz
    - TypeScript compiles, Biome lint passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create all 4 result section components</name>
  <files>components/result/ArchetypeReveal.tsx, components/result/FoundationalPatternsSection.tsx, components/result/WatchoutsSection.tsx, components/result/CulturalSection.tsx</files>
  <action>
Create `components/result/ArchetypeReveal.tsx`:
- Props: `archetype: Archetype` (from types.ts)
- Renders the dramatic reveal moment:
  - Eyebrow text: "You were raised by:" (small uppercase tracking, text-stone-400)
  - Archetype name: large display font (Cormorant Garamond via `style={{ fontFamily: "var(--font-display)" }}`), `text-4xl sm:text-5xl md:text-6xl`, text-stone-900
  - Tagline: e.g., "You grew up anchored." — text-xl italic, text-stone-600
  - Headline from foundationalPatterns: the one-sentence anchor, rendered as a lead paragraph, text-lg text-stone-700, max-w-2xl centered
- Full viewport height (`min-h-[80dvh]`), centered layout, amber-50 background
- Chapter-like visual section feel: generous padding (py-20 sm:py-28), breathing room

Create `components/result/FoundationalPatternsSection.tsx`:
- Props: `patterns: ArchetypeContent` (from types.ts)
- Section header: "What you inherited" in display font, with the headline sentence below
- Render all 5 themes as rich, styled list items (not plain `<ul><li>` bullets). Each theme is a paragraph-length string — render as a card or visually distinct block with left border accent (border-amber-400) and padding
- Research anchor at the bottom: italicized text-stone-500, smaller font
- Citations rendered as a collapsible section (use HTML `<details>/<summary>` for zero JS): "References" summary, then each citation as "Researcher, WorkTitle (Year) — relevanceNote"
- Background: `bg-white` section for alternating contrast
- Max-width container: `max-w-3xl mx-auto`, generous padding

Create `components/result/WatchoutsSection.tsx`:
- Props: `watchouts: ArchetypeContent` (from types.ts)
- Section header: "What to watch for" in display font, with headline (which includes "That served you then. It may not serve you now.")
- Render all 5 watchout themes identically to patterns section but with a different accent color (border-stone-400 instead of amber)
- Research anchor and citations same pattern as patterns section
- Background: `bg-stone-50` for alternating contrast

Create `components/result/CulturalSection.tsx`:
- Props: `overlay: CulturalOverlay`, `displayName: string` (e.g., "The East Asian Lens")
- This is the PAGE CLOSER — appears after Blueprint CTA as the final section
- Section header: display font, `displayName` (e.g., "The East Asian Lens")
- Three sub-sections:
  1. `expressionModifier` — narrative intro paragraph, text-stone-700, italic styling
  2. "What this gave you" — `strengthsInContext[]` as styled bullet list with amber accent
  3. "What this may have cost you" — `tensionsInContext[]` as styled bullet list with stone accent
- Background: `bg-amber-50` for warm closing
- Bottom padding extra generous (pb-24) since this closes the page

All components:
- Named exports (`export function ComponentName`)
- Server Components (no "use client" — these are pure presentational)
- Display font on all section headers via inline style
- Responsive: mobile-first Tailwind, readable on 375px width
- Line length limited by max-w-3xl or max-w-2xl for readability (65-75 chars)
- Section spacing: `py-16 sm:py-20` for chapter-like breathing room
- Run `npx biome check --write` on all 4 files
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit --pretty 2>&1 | head -30 && npx biome check components/result/ 2>&1 | tail -5 && npm run build 2>&1 | tail -15</automated>
    <manual>Visit /result?session={valid-session-uuid} and confirm all sections render with full content</manual>
  </verify>
  <done>
    - 4 Server Components created in components/result/
    - ArchetypeReveal shows archetype name prominently with tagline (RSLT-01)
    - FoundationalPatternsSection displays all 5 patterns in full (RSLT-07)
    - WatchoutsSection displays all 5 watchouts in full (RSLT-08)
    - CulturalSection displays cultural overlay content conditionally (RSLT-03, RSLT-09)
    - Production build passes
    - All components use display font, editorial warm palette, chapter-like spacing
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no TypeScript errors
2. `npx biome check lib/result/ components/result/ app/result/` — 0 lint errors
3. `npm run build` passes — /result route generates correctly
4. Navigate to `/result` (no session) — shows error state with link to quiz
5. Navigate to `/result?session=invalid-uuid` — shows error state
6. Navigate to `/result?session={real-completed-session}` — shows full archetype result with all sections
</verification>

<success_criteria>
- Result page fetches and renders archetype data from a real Supabase session
- All 4 content sections display complete data — no blurred, gated, or truncated content
- Cultural section appears conditionally based on user's quiz selection
- Page follows editorial warm palette and display font conventions from Phase 4
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-mirror-result-page/03-01-SUMMARY.md`
</output>
