---
phase: 03-mirror-result-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/result/BlueprintCTA.tsx
  - components/result/StickyBlueprintBar.tsx
  - app/blueprint/page.tsx
  - app/api/blueprint-interest/route.ts
  - supabase/migrations/20260226000000_blueprint_interest.sql
autonomous: true
requirements: [RSLT-05, RSLT-10]

must_haves:
  truths:
    - "An inline Blueprint CTA section appears on the result page with concrete messaging about what The Blueprint delivers"
    - "A sticky bottom bar CTA appears while scrolling the result page, promoting Blueprint launch notification"
    - "A dedicated /blueprint coming-soon page exists with email capture for launch notification"
    - "Blueprint interest emails are stored in a standalone Supabase table"
    - "The API route handles email submission for Blueprint notifications"
  artifacts:
    - path: "components/result/BlueprintCTA.tsx"
      provides: "Inline Blueprint coming-soon CTA section for result page"
      exports: ["BlueprintCTA"]
    - path: "components/result/StickyBlueprintBar.tsx"
      provides: "Sticky bottom bar CTA visible during scroll"
      exports: ["StickyBlueprintBar"]
    - path: "app/blueprint/page.tsx"
      provides: "Coming-soon page for The Blueprint (Product 2)"
      exports: ["BlueprintPage (default)"]
    - path: "app/api/blueprint-interest/route.ts"
      provides: "POST endpoint to store Blueprint notification emails"
      exports: ["POST"]
    - path: "supabase/migrations/20260226000000_blueprint_interest.sql"
      provides: "blueprint_interest_emails table"
  key_links:
    - from: "components/result/BlueprintCTA.tsx"
      to: "/blueprint"
      via: "Link to coming-soon page"
      pattern: "href.*blueprint"
    - from: "app/blueprint/page.tsx"
      to: "app/api/blueprint-interest/route.ts"
      via: "Client-side form submission"
      pattern: "fetch.*api/blueprint-interest"
    - from: "app/api/blueprint-interest/route.ts"
      to: "supabase/migrations/20260226000000_blueprint_interest.sql"
      via: "INSERT into blueprint_interest_emails"
      pattern: "blueprint_interest_emails.*insert"
---

<objective>
Build the Blueprint (Product 2) CTA components and coming-soon page. This includes: an inline CTA section for the result page (between Watchouts and Cultural), a sticky bottom bar visible during scroll, a dedicated /blueprint coming-soon page with email capture, and the API/database layer for storing Blueprint notification interest.

Purpose: Convert Mirror users into Blueprint pre-launch interest — capture email at peak engagement when users feel most seen.
Output: 2 CTA components + /blueprint page + API route + database migration
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mirror-result-page/03-CONTEXT.md
@.planning/phases/03-mirror-result-page/03-RESEARCH.md
@lib/supabase/server.ts
@components/quiz/EmailCaptureScreen.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Blueprint database table and API route</name>
  <files>supabase/migrations/20260226000000_blueprint_interest.sql, app/api/blueprint-interest/route.ts</files>
  <action>
Create `supabase/migrations/20260226000000_blueprint_interest.sql`:
- Create `blueprint_interest_emails` table:
  ```sql
  CREATE TABLE blueprint_interest_emails (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email TEXT NOT NULL,
    source TEXT NOT NULL DEFAULT 'result-page',  -- 'result-page' | 'blueprint-page' | 'sticky-bar'
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
  );
  ```
- Add unique constraint on email: `CREATE UNIQUE INDEX blueprint_interest_emails_email_idx ON blueprint_interest_emails (email);`
- Enable RLS: `ALTER TABLE blueprint_interest_emails ENABLE ROW LEVEL SECURITY;`
- Add INSERT policy that allows service_role only (no anon inserts — all writes go through API route with service_role):
  ```sql
  -- No anon policies needed — writes go through API route using service_role
  -- RLS enabled but no policies = deny all for anon users (correct for a server-write-only table)
  ```

Create `app/api/blueprint-interest/route.ts`:
- POST handler accepting JSON body `{ email: string, source?: string }`
- Validate email using a simple regex (no need for zod on server — lightweight validation)
- Use `createAdminClient()` from `lib/supabase/server.ts` to INSERT into `blueprint_interest_emails`
- Handle duplicate email gracefully: use `ON CONFLICT (email) DO NOTHING` via Supabase `.upsert()` with `onConflict: "email"` and `ignoreDuplicates: true`
- Return `{ success: true }` on 200, or `{ error: string }` on 400/500
- Source defaults to `"result-page"` if not provided

Run `npx supabase db push` to apply migration (if Supabase CLI available) or note migration for manual application.
Run `npx biome check --write` on the route file.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit --pretty 2>&1 | head -30 && npx biome check app/api/blueprint-interest/route.ts 2>&1 | tail -5</automated>
    <manual>Test API: curl -X POST http://localhost:3000/api/blueprint-interest -H "Content-Type: application/json" -d '{"email":"test@example.com","source":"result-page"}' returns success</manual>
  </verify>
  <done>
    - `blueprint_interest_emails` table migration exists and is ready to apply
    - API route accepts POST with email and source, returns success/error
    - Duplicate emails handled gracefully (no error, no duplicate rows)
    - TypeScript compiles, Biome passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BlueprintCTA, StickyBlueprintBar, and /blueprint page</name>
  <files>components/result/BlueprintCTA.tsx, components/result/StickyBlueprintBar.tsx, app/blueprint/page.tsx</files>
  <action>
Create `components/result/BlueprintCTA.tsx`:
- Server Component (no "use client" — static content with a link)
- Named export: `export function BlueprintCTA()`
- Visual design: a dedicated section between Watchouts and Cultural on the result page
- Content structure:
  - Section header: "Go deeper with The Blueprint" in display font
  - Concrete product description (per context decision): "A personalized action plan built from your result — including partner dynamics guidance, healing exercises, and research-backed strategies tailored to your archetype."
  - Three feature previews as small cards or icons: (1) "Personalized Action Plan", (2) "Partner Dynamics Guide", (3) "Healing Exercises"
  - CTA button: "Get notified when it launches" linking to `/blueprint`
  - Status badge: "Coming Soon" label
- Background: `bg-stone-900` (dark section for contrast, matching ProductLadder from landing page)
- Text: `text-white` primary, `text-stone-300` secondary
- Padding: `py-16 sm:py-20`, `max-w-3xl mx-auto`

Create `components/result/StickyBlueprintBar.tsx`:
- Client Component ("use client") — needs scroll state and form interactivity
- Named export: `export function StickyBlueprintBar()`
- Fixed bottom bar: `position: fixed; bottom: 0; left: 0; right: 0; z-index: 40`
- Appears only after scrolling past a threshold (400px from top) — use a `useEffect` with scroll listener or IntersectionObserver
- Design: thin bar (py-3) with `bg-stone-900/95 backdrop-blur-sm` and `border-t border-stone-700`
- Content: "The Blueprint is coming" text + small "Notify me" button that links to `/blueprint`
- Mobile: stacks text and button vertically; desktop: horizontal layout
- Hidden when email gate is open — accept optional `hidden` prop: if `hidden={true}`, render nothing (the gate overlay in Plan 03 will pass this prop)
- `prefers-reduced-motion`: respect via `@media (prefers-reduced-motion: reduce)` — skip entrance animation
- z-index: 40 (below email gate at z-50)

Create `app/blueprint/page.tsx`:
- Server Component page with a Client Component form island
- Metadata: `title: "The Blueprint — Coming Soon | Raising Giants"`, description about the Blueprint product
- Full-page coming-soon design on `bg-amber-50`:
  - Display font headline: "The Blueprint"
  - Subhead: "A personalized guide to bridge what you inherited with what you want to build."
  - Three value props (matching inline CTA): personalized action plan, partner dynamics, healing exercises
  - Email capture form (Client Component embedded inline with "use client"):
    - Uses react-hook-form + zod for email validation (same pattern as EmailCaptureScreen)
    - On submit: POST to `/api/blueprint-interest` with `source: "blueprint-page"`
    - Show success state after submission: "You're on the list! We'll notify you when The Blueprint launches."
    - Show error state on failure
  - Link back to home: "← Back to Raising Giants"
- The email capture form should be extracted as a small inline Client Component within the same file OR as a separate `components/result/BlueprintEmailForm.tsx` — use Claude's discretion, but keep it simple
- Run `npx biome check --write` on all files
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit --pretty 2>&1 | head -30 && npx biome check components/result/BlueprintCTA.tsx components/result/StickyBlueprintBar.tsx app/blueprint/ 2>&1 | tail -5 && npm run build 2>&1 | tail -15</automated>
    <manual>Navigate to /blueprint page and verify email capture form works; check that sticky bar appears on result page scroll</manual>
  </verify>
  <done>
    - BlueprintCTA renders concrete product messaging with "Coming Soon" label (RSLT-05)
    - StickyBlueprintBar appears on scroll, hidden when gate is open
    - /blueprint page exists with email capture form (RSLT-10)
    - Email submissions stored in blueprint_interest_emails table
    - Production build passes with all new routes
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no TypeScript errors
2. `npx biome check components/result/ app/blueprint/ app/api/blueprint-interest/` — 0 lint errors
3. `npm run build` passes — /blueprint and /api/blueprint-interest routes generate
4. Navigate to `/blueprint` — shows coming-soon page with email capture
5. Submit email on /blueprint — success state appears, row in database
6. Blueprint CTA section renders with concrete product messaging
</verification>

<success_criteria>
- Blueprint CTA clearly names what Product 2 delivers (personalized plan, partner guide, exercises)
- /blueprint page captures email for launch notification and stores it
- Sticky bar appears during scroll without interfering with reading experience
- No paywall, no $14 purchase button anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/03-mirror-result-page/03-02-SUMMARY.md`
</output>
