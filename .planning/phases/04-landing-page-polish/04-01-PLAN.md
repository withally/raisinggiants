---
phase: 04-landing-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/landing/HeroSection.tsx
  - components/landing/ProductLadder.tsx
  - components/landing/KOLCredibility.tsx
  - components/landing/SecondaryHero.tsx
  - components/landing/Footer.tsx
  - app/page.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05

must_haves:
  truths:
    - "Navigating to / shows a full landing page with hero, product ladder, KOL credibility, secondary CTA, and footer — not the old placeholder"
    - "At least 6 named KOL researchers appear with specific work attribution (name + work title)"
    - "All three products (Mirror, Blueprint, Partner Match) are visible with their descriptions and status"
    - "The primary quiz CTA links to /quiz and is visible above the fold on mobile (375px viewport)"
    - "No mention of Sophia's personal credentials appears anywhere on the landing page"
    - "The entire page is mobile-responsive — stacked layouts on mobile, multi-column on desktop"
  artifacts:
    - path: "components/landing/HeroSection.tsx"
      provides: "Above-the-fold hero with headline, subheadline, primary CTA"
      min_lines: 30
    - path: "components/landing/ProductLadder.tsx"
      provides: "Three-product journey visualization: Mirror, Blueprint, Partner Match"
      min_lines: 40
    - path: "components/landing/KOLCredibility.tsx"
      provides: "Named researcher grid with work attribution"
      min_lines: 50
    - path: "components/landing/SecondaryHero.tsx"
      provides: "Closing CTA section before footer"
      min_lines: 15
    - path: "components/landing/Footer.tsx"
      provides: "Minimal brand footer"
      min_lines: 10
    - path: "app/page.tsx"
      provides: "Landing page composing all sections"
      min_lines: 20
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/*"
      via: "import and composition"
      pattern: "import.*from.*components/landing"
    - from: "components/landing/HeroSection.tsx"
      to: "/quiz"
      via: "Link href"
      pattern: "href=.*/quiz"
    - from: "components/landing/KOLCredibility.tsx"
      to: "KOL data"
      via: "inline researcher data array"
      pattern: "(Gottman|Siegel|Baumrind|Ainsworth|Kennedy|Tsabary)"
---

<objective>
Build the complete production landing page for Raising Giants — The Mirror. Replace the placeholder `app/page.tsx` with a full, credibility-forward, mobile-responsive landing page composed of distinct section components.

Purpose: The landing page is the first impression — it must communicate the product's research-backed nature, present the full product ladder (Mirror/Blueprint/Partner Match), establish trust through named KOL researchers, and drive visitors into the quiz at `/quiz`. Per user decisions, Sophia is NOT on the landing page; credibility comes from the framework's KOL research attribution.

Output: 5 section components in `components/landing/` and a rewritten `app/page.tsx` that composes them into a complete landing page.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-landing-page-polish/04-CONTEXT.md
@.planning/phases/04-landing-page-polish/04-RESEARCH.md
@.agents/skills/frontend-design/SKILL.md
@app/layout.tsx
@app/globals.css
@lib/archetypes/archetypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page section components</name>
  <files>
    components/landing/HeroSection.tsx
    components/landing/ProductLadder.tsx
    components/landing/KOLCredibility.tsx
    components/landing/SecondaryHero.tsx
    components/landing/Footer.tsx
  </files>
  <action>
Create `components/landing/` directory and build 5 Server Components (no "use client" needed — all static content):

**HeroSection.tsx** — Above-the-fold section:
- Headline that speaks to the larger aspiration: helping people become better parents by understanding their own upbringing. Per CONTEXT.md: warm, empathetic, "you've always sensed this" energy. The quiz is "the starting point of a journey, not the destination."
- Subheadline with supporting copy that grounds the product in research from top parenting experts (LAND-01)
- Primary CTA button linking to `/quiz` using `next/link` — must be prominent, full-width on mobile (LAND-03)
- Background: extend the warm amber/off-white palette (`bg-amber-50` or similar warm base)
- Mobile: single column, generous vertical spacing, CTA visible without scrolling on 375px viewport
- Desktop: can use asymmetric layout or centered hero — Claude's discretion per CONTEXT.md

**ProductLadder.tsx** — Three-product journey section:
- Display all three products explicitly per user decision:
  - **The Mirror** — "Understand your upbringing" — Free — with quiz CTA (links to `/quiz`)
  - **The Blueprint** — "Understand your tendencies and aspirations" — Coming Soon label
  - **Partner Match** — "Have a dialogue with your partner about parenting" — Coming Soon label
- Layout: stacked cards on mobile (vertical progression suggesting journey), horizontal row on `lg:` desktop
- Visual progression: make it feel like a natural journey from Mirror to Blueprint to Partner Match (e.g., numbered steps, connecting lines, arrow indicators, or progressive visual weight)
- Only Mirror has a live CTA link to `/quiz`; Blueprint and Partner Match show "Coming Soon" badges — no dead links

**KOLCredibility.tsx** — Named researcher credibility grid (LAND-02, LAND-05):
- Display 8 named researchers with specific work attribution (researcher name + one-line framework contribution + work title/year):
  1. Diana Baumrind — authoritative parenting framework — "Child care practices anteceding three patterns of preschool behavior" (1967)
  2. John Gottman — emotional coaching and repair — "Raising an Emotionally Intelligent Child" (1997)
  3. Daniel J. Siegel — interpersonal neurobiology — "The Developing Mind" (2012)
  4. Mary Ainsworth — attachment theory — Strange Situation (1978)
  5. Dr. Becky Kennedy — repair as cornerstone of connection — "Good Inside" (2022)
  6. Shefali Tsabary — conscious parenting — "The Conscious Parent" (2010)
  7. Bessel van der Kolk — intergenerational trauma — "The Body Keeps the Score" (2014)
  8. Ronald P. Rohner — cross-cultural parental acceptance — IPARTheory/PARQ (60+ cultures)
- Researcher data is defined as a const array within this component (not imported from archetypes.ts — the landing page needs curated display data, not the full archetype citation format)
- Layout: 2 columns on mobile, 3-4 columns on `lg:` desktop
- Section intro copy positions this as "Grounded in decades of research from the world's leading parenting scientists" or similar — emphasizing the research, not a single practitioner (LAND-05 via KOL mechanism, not Sophia)
- **CRITICAL: No mention of Sophia anywhere in this component or any landing page component**

**SecondaryHero.tsx** — Closing CTA section:
- Short, emotionally resonant closing line reinforcing the "start your journey" message
- Repeat quiz CTA button linking to `/quiz` (LAND-03 — CTA repeated after credibility established)
- Visually distinct from the hero (e.g., different background shade, subtle pattern, or contrast section)

**Footer.tsx** — Minimal brand footer:
- Brand name "Raising Giants" with copyright year
- No complex navigation for v1 — just the brand mark
- Muted styling (`text-stone-500` range)

**Design direction (Claude's discretion per CONTEXT.md):**
- Read the frontend-design SKILL.md for aesthetic guidance
- Emotional register: warm, reflective, personal, slightly literary — NOT clinical, corporate, or childish
- Color palette: extend the existing warm amber/stone palette; can introduce additional warm tones (muted sage, warm cream, soft terracotta) as accents
- Typography: use the existing Geist Sans for now (display font upgrade happens in Plan 04-02)
- Animations: CSS-only if used (no framer-motion); prefer subtle reveals or gentle hover states
- Spatial composition: generous whitespace, asymmetric where it adds character, grid-breaking welcome
- Avoid: purple gradients, generic startup landing page patterns, stock photo layouts, Inter/Roboto

**Mobile-first (LAND-04):**
- Build mobile-first using Tailwind responsive utilities (`sm:`, `md:`, `lg:`)
- Full-width CTAs on mobile (min-h-[44px] for Apple HIG compliance)
- Stack all multi-column layouts to single column on mobile
- Readable typography sizes on mobile (16px+ body text)
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && ls components/landing/HeroSection.tsx components/landing/ProductLadder.tsx components/landing/KOLCredibility.tsx components/landing/SecondaryHero.tsx components/landing/Footer.tsx && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify each component file exists and TypeScript compiles without errors</manual>
  </verify>
  <done>5 landing page section components exist in components/landing/, all type-check cleanly, contain no "use client" directives, and collectively cover hero/product-ladder/KOL-credibility/secondary-CTA/footer sections</done>
</task>

<task type="auto">
  <name>Task 2: Replace app/page.tsx with full landing page</name>
  <files>
    app/page.tsx
  </files>
  <action>
Replace the current placeholder `app/page.tsx` entirely with a Server Component that composes all 5 section components:

```tsx
import { HeroSection } from "@/components/landing/HeroSection";
import { ProductLadder } from "@/components/landing/ProductLadder";
import { KOLCredibility } from "@/components/landing/KOLCredibility";
import { SecondaryHero } from "@/components/landing/SecondaryHero";
import { Footer } from "@/components/landing/Footer";

export default function Home() {
  return (
    <main>
      <HeroSection />
      <ProductLadder />
      <KOLCredibility />
      <SecondaryHero />
      <Footer />
    </main>
  );
}
```

- The page is a pure Server Component — no "use client", no useState, no useEffect
- Section order follows the conversion funnel: hero (hook) → product ladder (context) → KOL credibility (trust) → secondary CTA (convert) → footer
- The `<main>` wrapper has no background color — individual sections own their backgrounds for visual rhythm (alternating warm shades)
- Ensure the page renders correctly with `npm run dev` and navigating to `http://localhost:3000/`
- Verify that clicking the CTA navigates to `/quiz`

Run `npm run lint` (Biome) and fix any lint issues. Biome enforces double-quote strings and semicolons — ensure all new components follow this convention.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit && npm run lint 2>&1 | tail -20</automated>
    <manual>Visit http://localhost:3000/ in browser and verify the full landing page renders with all 5 sections visible</manual>
  </verify>
  <done>app/page.tsx composes all 5 landing page sections, TypeScript compiles, Biome lint passes, and the page renders at / with hero, product ladder, KOL credibility, secondary CTA, and footer all visible</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors across entire project)
2. `npm run lint` passes (Biome formatting/lint clean)
3. `npm run build` succeeds (static page generates without errors)
4. Navigating to `/` shows the complete landing page — not the old "Your Parenting Blueprint" placeholder
5. At least 8 named KOL researchers visible with work attribution
6. Three products (Mirror, Blueprint, Partner Match) shown with correct status labels
7. Quiz CTA links to `/quiz` and is clickable
8. No mention of "Sophia" anywhere on the rendered page
9. Page layout stacks to single column on mobile viewport (375px)
</verification>

<success_criteria>
The landing page at `/` is a complete, production-quality, mobile-responsive page with: (1) an above-the-fold hero with warm headline and quiz CTA, (2) a product ladder showing Mirror/Blueprint/Partner Match, (3) a KOL credibility grid with 8 named researchers and specific work attribution, (4) a closing CTA section, (5) a minimal footer. The page compiles, lints, and builds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-landing-page-polish/04-01-SUMMARY.md`
</output>
