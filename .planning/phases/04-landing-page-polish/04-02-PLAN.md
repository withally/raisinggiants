---
phase: 04-landing-page-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/layout.tsx
  - app/globals.css
  - components/landing/HeroSection.tsx
  - components/landing/ProductLadder.tsx
  - components/landing/KOLCredibility.tsx
  - components/landing/SecondaryHero.tsx
  - components/landing/Footer.tsx
autonomous: false
requirements:
  - LAND-01
  - LAND-04

must_haves:
  truths:
    - "Browser tab and metadata show 'Raising Giants' and 'The Mirror' branding — not the old 'Your Parenting Blueprint'"
    - "The landing page uses a distinctive display font for headlines that elevates the design beyond the default Geist Sans"
    - "The page passes basic Lighthouse mobile performance audit (no render-blocking external font requests, no massive unoptimized images)"
    - "User confirms the landing page is visually polished and ready for launch"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Updated metadata (title, description, Open Graph) and display font loading via next/font/google"
      contains: "Raising Giants"
    - path: "app/globals.css"
      provides: "CSS custom property for display font and any landing-page-specific keyframes"
  key_links:
    - from: "app/layout.tsx"
      to: "next/font/google"
      via: "font import and CSS variable"
      pattern: "from.*next/font/google"
    - from: "components/landing/HeroSection.tsx"
      to: "display font CSS variable"
      via: "className using font-display"
      pattern: "font-\\[var\\(--font-display"
---

<objective>
Polish the landing page with correct branding metadata, a distinctive display font for headlines, and visual refinements. Then verify the complete landing page with the user.

Purpose: The current layout metadata still says "Your Parenting Blueprint" — this must be updated to "Raising Giants" before any public use. Adding a distinctive display font for headlines elevates the landing page from functional to memorable (per frontend-design SKILL.md: "distinctive display font with a refined body font"). The checkpoint ensures the user is satisfied with the visual result before the phase closes.

Output: Updated layout.tsx with correct metadata and display font, polished landing page components using the display font, and user sign-off on the visual result.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-landing-page-polish/04-CONTEXT.md
@.planning/phases/04-landing-page-polish/04-RESEARCH.md
@.planning/phases/04-landing-page-polish/04-01-SUMMARY.md
@.agents/skills/frontend-design/SKILL.md
@app/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update metadata and add display font</name>
  <files>
    app/layout.tsx
    app/globals.css
    components/landing/HeroSection.tsx
    components/landing/ProductLadder.tsx
    components/landing/KOLCredibility.tsx
    components/landing/SecondaryHero.tsx
    components/landing/Footer.tsx
  </files>
  <action>
**Update `app/layout.tsx` metadata:**
- Change `title` from "Your Parenting Blueprint" to "Raising Giants — The Mirror"
- Update `description` to reflect the parents-lens framing: something like "Understand the parenting you received. A free, research-backed assessment grounded in the work of the world's leading parenting scientists."
- Add Open Graph metadata:
  ```tsx
  openGraph: {
    title: "Raising Giants — The Mirror",
    description: "...", // same as description
    type: "website",
    siteName: "Raising Giants",
  }
  ```

**Add a display font for headlines:**
- Import a distinctive display font via `next/font/google` (same loading mechanism as Geist Sans — no external `<link>` tags, no render-blocking requests)
- Candidate fonts (Claude's discretion — pick the one that best matches the warm, reflective, slightly literary tone):
  - Playfair Display (editorial warmth, strong serif)
  - Lora (literary warmth, readable serif)
  - DM Serif Display (modern editorial, clean serif)
  - Cormorant Garamond (refined literary, elegant)
- Apply the font as a CSS variable (e.g., `--font-display`) on the `<body>` alongside the existing `--font-geist-sans`
- Add a `--font-display` reference in `globals.css` `@theme inline` if needed for Tailwind v4 utility class generation

**Apply display font to landing page components:**
- Update headline elements in HeroSection, ProductLadder section title, KOLCredibility section title, and SecondaryHero headline to use the display font (via `style={{ fontFamily: "var(--font-display)" }}` or a Tailwind utility if wired through @theme)
- Body text, CTAs, and smaller UI elements remain in Geist Sans
- The contrast between a characterful serif display font and clean sans-serif body text creates visual hierarchy and editorial warmth

**Visual polish pass (while touching the components):**
- Refine spacing, shadows, border treatments across all section components
- Ensure consistent use of the warm color palette
- Add any subtle CSS-only animations or transitions that enhance the experience (e.g., gentle hover lifts on cards, smooth color transitions on CTA buttons)
- Verify all interactive elements have min-h-[44px] touch targets

Run `npm run lint` and `npx tsc --noEmit` to ensure everything compiles and lints cleanly.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit && npm run lint 2>&1 | tail -20 && grep -c "Raising Giants" app/layout.tsx</automated>
    <manual>Visit http://localhost:3000/ and verify headlines use a serif display font distinct from body text Geist Sans</manual>
  </verify>
  <done>layout.tsx metadata shows "Raising Giants — The Mirror" title and updated description with Open Graph tags. A display font is loaded via next/font/google and applied to all landing page headlines. All components lint and type-check cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete landing page</name>
  <files>app/page.tsx</files>
  <action>
This is a human verification checkpoint. No automated work — human reviews the complete landing page built in Plan 04-01 and polished in Task 1 of this plan.

What was built: Complete landing page for Raising Giants — The Mirror with:
- Above-the-fold hero with warm headline, research-backed subheadline, and prominent quiz CTA
- Product ladder showing Mirror (free, live CTA), Blueprint (coming soon), Partner Match (coming soon)
- KOL credibility grid with 8 named researchers and specific work attribution
- Closing CTA section reinforcing the quiz invitation
- Minimal brand footer
- Distinctive serif display font for headlines
- Updated metadata (title: "Raising Giants — The Mirror")
- Fully mobile-responsive layout
  </action>
  <verify>
Start the dev server: `npm run dev`
Open http://localhost:3000/ in browser

**Desktop verification (>1024px viewport):**
- [ ] Hero section has a warm headline about understanding your upbringing / becoming a better parent
- [ ] Quiz CTA button is prominent and links to /quiz
- [ ] Product ladder shows Mirror / Blueprint / Partner Match with correct status labels
- [ ] KOL section shows at least 8 named researchers with work titles
- [ ] No mention of "Sophia" anywhere on the page
- [ ] Closing CTA section has another quiz link
- [ ] Footer shows "Raising Giants" branding
- [ ] Browser tab shows "Raising Giants — The Mirror"

**Mobile verification (use browser DevTools -> 375px iPhone viewport):**
- [ ] Quiz CTA is visible without scrolling (above the fold at 375px)
- [ ] Product ladder cards stack vertically
- [ ] KOL grid is 2 columns
- [ ] All tap targets are at least 44px
- [ ] Text is readable (no overflow, no truncation)

**Typography verification:**
- [ ] Headlines use a serif display font (different from the body text sans-serif)
- [ ] Body text remains in Geist Sans

**Click-through verification:**
- [ ] Click the primary quiz CTA — navigates to /quiz
- [ ] Navigate back to / — page state is correct

Type "approved" if the landing page is ready, or describe any issues to address.
  </verify>
  <done>User has approved the landing page visual result — confirms hero, product ladder, KOL credibility, typography, mobile responsiveness, and metadata are all launch-ready</done>
</task>

</tasks>

<verification>
1. `app/layout.tsx` title contains "Raising Giants" (not "Your Parenting Blueprint")
2. `app/layout.tsx` contains Open Graph metadata
3. A display font is imported via `next/font/google` and applied as a CSS variable
4. Landing page headlines visually use a serif/display font distinct from body text
5. `npx tsc --noEmit` passes
6. `npm run lint` passes
7. `npm run build` succeeds
8. User has approved the visual result at the checkpoint
</verification>

<success_criteria>
The landing page has correct "Raising Giants — The Mirror" branding in metadata, distinctive display typography for headlines loaded via next/font/google, visual polish across all sections, and explicit user approval at the checkpoint confirming the page is launch-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/04-landing-page-polish/04-02-SUMMARY.md`
</output>
