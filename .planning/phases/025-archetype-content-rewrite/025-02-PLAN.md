---
phase: 025-archetype-content-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/archetypes/cultural-overlays.ts
  - app/result/page.tsx
autonomous: true
requirements:
  - COPY-04

must_haves:
  truths:
    - "All 45 cultural overlays frame from the receiver perspective — what this context gave you and what it may have cost you"
    - "No expressionModifier, strengthsInContext, or tensionsInContext string implies the user is the active parent"
    - "Cultural overlay expressionModifiers maintain factual/observational register (not personal second-person)"
    - "Result page placeholder text uses parents-lens framing"
  artifacts:
    - path: "lib/archetypes/cultural-overlays.ts"
      provides: "45 fully rewritten cultural overlays (9 archetypes x 5 cultures) with receiver perspective"
      contains: "you grew up"
    - path: "app/result/page.tsx"
      provides: "Placeholder result page with corrected parents-lens description text"
      contains: "parenting you received"
  key_links:
    - from: "lib/archetypes/cultural-overlays.ts"
      to: "lib/archetypes/types.ts"
      via: "CulturalOverlay type interface — string fields unchanged, only values rewritten"
      pattern: "expressionModifier|strengthsInContext|tensionsInContext"
---

<objective>
Rewrite all cultural overlay content in `cultural-overlays.ts` from parent-doing to receiver perspective, and fix secondary file framing issues.

Purpose: The 45 cultural overlays (9 archetypes x 5 cultural contexts) currently describe what the active parent does in each cultural setting. This plan rewrites every overlay to describe what the child received — strengths framed as "what this gave you" and tensions framed as "what this may have cost you" per CONTEXT.md. Also fixes the result page placeholder text to use parents-lens framing.

Output: `cultural-overlays.ts` with all 45 overlays rewritten to receiver perspective. `app/result/page.tsx` with corrected placeholder description.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/025-archetype-content-rewrite/025-CONTEXT.md
@.planning/phases/025-archetype-content-rewrite/025-RESEARCH.md
@lib/archetypes/cultural-overlays.ts
@lib/archetypes/types.ts
@app/result/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite all 45 cultural overlay entries to receiver perspective</name>
  <files>lib/archetypes/cultural-overlays.ts</files>
  <action>
Rewrite `lib/archetypes/cultural-overlays.ts` content strings to shift from parent-doing to child-receiving perspective. All 45 overlays (9 archetypes x 5 cultural contexts) must be rewritten. The TypeScript structure, imports, exports, culturalContext labels, and ArchetypeId keys stay EXACTLY as-is. Only string values in content fields change.

**File-level comments (lines 1-28):** Update references to "parenting archetype" framing. Change "how each archetype's core tendencies manifest" to "how the parenting you received expressed differently across cultural contexts." The Fierce Guardian example in the comment should be reframed: instead of "the protective instinct expresses through academic pressure" → "the protective instinct you experienced may have expressed through academic pressure and family honor rather than physical vigilance."

**For each of the 45 overlay entries, rewrite:**

1. **`expressionModifier`** — Keep factual/observational register per CONTEXT.md. This should NOT be personal second-person ("you felt..."). Instead, describe how the archetype's qualities manifested in this cultural context from the child's perspective. Shift from "The Steady Anchor's calm and structure are tested by..." to something like "Steadiness in East Asian collectivist contexts tended to express through behavioral reliability and provision rather than verbal reassurance. Growing up with this, calm often read as respectful restraint — love understood through action before words."

2. **`strengthsInContext`** (2-3 bullets per overlay, ~100+ total) — Shift to receiver perspective: "what this gave you." Instead of "Behavioral reliability and predictability are deeply culturally valued..." write "The behavioral reliability you grew up with aligned with group harmony — giving you a deep sense of belonging." Per CONTEXT.md example.

3. **`tensionsInContext`** (2-3 bullets per overlay, ~100+ total) — Shift to receiver perspective: "what this may have cost you." Instead of "The archetype's low autonomy support may be more explicitly challenged..." write "Emotional expressiveness may have felt unfamiliar growing up — leaving you with fewer models for naming what you feel." Per CONTEXT.md example.

**Cultural sensitivity directives from CONTEXT.md:**
- Gently acknowledge where cultural norms reinforced patterns: "In this context, this pattern was often reinforced by the community around you" — but use judgment per overlay on where this adds value vs risks offense
- Maintain factual/observational tone in expressionModifier (not personal second-person)
- No hierarchy between cultural contexts — none framed as better or worse
- Western individualist treated as one context among equals (not the default)

**The Resilient Striver cultural overlays** need special care — the earned-security/healing narrative shifts to "growing up with people who had done their own healing work." In each cultural context, frame how the child experienced that healing journey differently.

**DO NOT TOUCH:**
- culturalContext string labels (CTX_EAST_ASIAN, etc.)
- ArchetypeId keys
- Import/export statements
- TypeScript types
- The overall array/object structure

**Tone:** "Warm expert friend" — empathetic, factual, normalizing. Cultural sensitivity paramount. No stereotyping. Every overlay describes contextual tendencies, not deterministic descriptions.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit 2>&1 | head -20 && echo "---" && node -e "
const fs = require('fs');
const content = fs.readFileSync('lib/archetypes/cultural-overlays.ts', 'utf8');
const wrongPatterns = ['the parent', 'as a parent', 'the archetype\\'s', 'parenting style'];
const hits = wrongPatterns.filter(p => {
  const regex = new RegExp(p, 'gi');
  const matches = content.match(regex);
  return matches && matches.length > 0;
});
if (hits.length > 0) { console.log('WARN — potentially wrong-framing patterns found:', hits); console.log('Manual review recommended — some may be in comments which is OK'); }
else { console.log('PASS — no wrong-framing patterns found in content'); }
// Count overlay entries
const overlayCount = (content.match(/expressionModifier:/g) || []).length;
console.log('Overlay entries found:', overlayCount, '/ 45 expected');
if (overlayCount < 45) { console.log('FAIL — missing overlay entries'); process.exit(1); }
console.log('PASS — all 45 overlays present');
"</automated>
    <manual>Read 2-3 cultural overlays across different archetypes and cultures to confirm receiver perspective and cultural sensitivity</manual>
  </verify>
  <done>All 45 cultural overlays rewritten to receiver perspective. strengthsInContext frames as "what this gave you." tensionsInContext frames as "what this may have cost you." expressionModifier maintains factual/observational register. TypeScript compiles. No parent-as-subject framing remains in content strings.</done>
</task>

<task type="auto">
  <name>Task 2: Fix result page placeholder and file comments for parents-lens framing</name>
  <files>app/result/page.tsx</files>
  <action>
Fix secondary framing issues in `app/result/page.tsx` (the placeholder result page that Phase 3 will replace):

1. **Line 7 description:** Change `"Your personalised parenting archetype result from The Mirror assessment."` to `"Discover the parenting patterns you inherited — your personalised Mirror result."` — aligns with parents-lens.

2. **Line 30-32 placeholder text:** Change `"your parenting archetype breakdown, dimension scores, and tailored insights grounded in research"` to `"the parenting archetype that shaped you, the patterns you inherited, and tailored insights grounded in research"` — removes "your parenting archetype" active-parent framing.

These are small string changes in a placeholder page. Phase 3 will rebuild this page entirely, but the placeholder should not mislead during development.

Run Biome format after changes: `npx biome check --write app/result/page.tsx`
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit 2>&1 | head -5 && npx biome check app/result/page.tsx 2>&1 | tail -3</automated>
    <manual>Read app/result/page.tsx — placeholder text should reference "parenting patterns you inherited" not "your parenting archetype"</manual>
  </verify>
  <done>Result page placeholder description and metadata use parents-lens framing. Biome passes. TypeScript compiles.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors introduced across either file
2. All 45 cultural overlays present (expressionModifier count = 45)
3. Grep for wrong-framing patterns (`the parent`, `as a parent`, `the archetype's`, `parenting style`) returns zero hits in cultural-overlays.ts content strings (OK if in file comments explaining the historical change)
4. strengthsInContext bullets across all overlays use receiver perspective ("what this gave you")
5. tensionsInContext bullets across all overlays use receiver perspective ("what this may have cost you")
6. Result page placeholder uses parents-lens framing
7. `npx biome check` passes on all modified files
</verification>

<success_criteria>
- cultural-overlays.ts compiles without errors
- 45/45 overlays rewritten to receiver perspective
- expressionModifier maintains factual/observational register (not personal second-person)
- strengthsInContext reads as "what this gave you"
- tensionsInContext reads as "what this may have cost you"
- Cultural sensitivity maintained — no stereotyping, no hierarchy
- Result page placeholder text corrected
- All modified files pass Biome check
</success_criteria>

<output>
After completion, create `.planning/phases/025-archetype-content-rewrite/025-02-SUMMARY.md`
</output>
