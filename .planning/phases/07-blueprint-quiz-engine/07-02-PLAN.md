---
phase: 07-blueprint-quiz-engine
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - components/quiz/ParentStatusSelector.tsx
  - components/quiz/BlueprintQuizShell.tsx
  - app/blueprint/quiz/page.tsx
autonomous: true
requirements: [BQUIZ-01, BQUIZ-02, BQUIZ-03, BQUIZ-06]

must_haves:
  truths:
    - "User sees a parent-status selector screen before the quiz begins with three options"
    - "User can complete the full Blueprint quiz with card-style UI, progress bar, back navigation, and teal/green accent"
    - "Quiz state resumes from localStorage if user leaves mid-quiz and returns"
    - "Blueprint quiz does not interfere with Mirror quiz state in any way"
    - "Scoring uses existing computeDimensionProfile + getResult pipeline with Blueprint questions"
  artifacts:
    - path: "components/quiz/ParentStatusSelector.tsx"
      provides: "Pre-quiz branching screen with 3 parent status options"
      min_lines: 30
    - path: "components/quiz/BlueprintQuizShell.tsx"
      provides: "Full quiz orchestrator adapted from QuizShell for Blueprint"
      min_lines: 200
    - path: "app/blueprint/quiz/page.tsx"
      provides: "Blueprint quiz route with NuqsAdapter + Suspense"
      min_lines: 15
  key_links:
    - from: "components/quiz/BlueprintQuizShell.tsx"
      to: "stores/blueprintStore.ts"
      via: "useBlueprintStore hook"
      pattern: "useBlueprintStore"
    - from: "components/quiz/BlueprintQuizShell.tsx"
      to: "lib/quiz/blueprint-questions.ts"
      via: "getBlueprintQuestions import"
      pattern: "getBlueprintQuestions"
    - from: "components/quiz/BlueprintQuizShell.tsx"
      to: "lib/quiz/compute-profile.ts"
      via: "computeDimensionProfile with Blueprint questions override"
      pattern: "computeDimensionProfile\\(.*answers.*getBlueprintQuestions"
    - from: "components/quiz/BlueprintQuizShell.tsx"
      to: "app/api/bp-quiz-session/route.ts"
      via: "fetch POST/PATCH to /api/bp-quiz-session"
      pattern: "fetch.*api/bp-quiz-session"
    - from: "components/quiz/BlueprintQuizShell.tsx"
      to: "components/quiz/ParentStatusSelector.tsx"
      via: "renders ParentStatusSelector when parentStatus is null"
      pattern: "ParentStatusSelector"
---

<objective>
Build the Blueprint quiz UI: parent-status selector, quiz orchestrator shell, and quiz page route.

Purpose: This is the interactive core of Phase 7 -- the user-facing quiz experience. It composes existing quiz components (QuizCard, QuizProgress, ProcessingScreen) with a new Blueprint-flavored orchestrator, adding the parent-status gating screen and teal/green visual identity.
Output: 3 new files delivering the complete Blueprint quiz flow from parent-status selection through processing screen.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-blueprint-quiz-engine/07-CONTEXT.md
@.planning/phases/07-blueprint-quiz-engine/07-RESEARCH.md
@.planning/phases/07-blueprint-quiz-engine/07-01-SUMMARY.md

Key source files to reference (read these before implementing):
@components/quiz/QuizShell.tsx (Mirror orchestrator — THE primary reference; adapt this for Blueprint)
@components/quiz/QuizCard.tsx (reuse directly — pass Blueprint accent colors)
@components/quiz/QuizProgress.tsx (reuse directly — pass Blueprint accentLight/accentDark)
@components/quiz/ProcessingScreen.tsx (reuse or lightly adapt for Blueprint closing tone)
@components/quiz/EmailCaptureScreen.tsx (reference for email capture pattern)
@app/quiz/page.tsx (Mirror quiz page — replicate NuqsAdapter + Suspense pattern)
@stores/blueprintStore.ts (from Plan 01 — Blueprint Zustand store)
@lib/quiz/blueprint-questions.ts (from Plan 01 — getBlueprintQuestions, BLUEPRINT_SECTIONS, TOTAL_BLUEPRINT_QUESTIONS)
@lib/quiz/blueprint-section-palette.ts (from Plan 01 — getBlueprintSectionColor)
@lib/quiz/compute-profile.ts (computeDimensionProfile — pass Blueprint questions as override)
@lib/quiz/scoring-matrix.ts (getResult — unchanged, works with any DimensionProfile)
@lib/landing/palette.ts (ff, ffSerif font family references)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ParentStatusSelector and BlueprintQuizShell components</name>
  <files>
    components/quiz/ParentStatusSelector.tsx
    components/quiz/BlueprintQuizShell.tsx
  </files>
  <action>
    **ParentStatusSelector** (`components/quiz/ParentStatusSelector.tsx`):
    A "use client" full-screen selector component. NOT a quiz card — a distinct pre-quiz branching screen per CONTEXT.md locked decision.

    - Accept `onSelect: (status: ParentStatus) => void` prop
    - Three large tappable option cards:
      1. "I'm a parent now" / sublabel: "You have a child or children" / id: 'current-parent'
      2. "I'm expecting" / sublabel: "A baby is on the way" / id: 'expecting'
      3. "Planning for someday" / sublabel: "Not yet, but thinking about it" / id: 'planning'
    - Headline: "First -- a quick question" (warm & intentional per CONTEXT.md)
    - Full viewport height, centered content, cream background (#FAFAF7)
    - Cards should have white bg, rounded-2xl, border-[#E8E4DF], hover state using Blueprint teal (e.g., hover:border-[#2D8B7A] hover:bg-[#F0FAF8])
    - 44px minimum touch target (Apple HIG compliance, established pattern)
    - Use `ff` from `@/lib/landing/palette` for font family consistency

    **BlueprintQuizShell** (`components/quiz/BlueprintQuizShell.tsx`):
    Adapt `components/quiz/QuizShell.tsx` for the Blueprint quiz. This is a composition, NOT a copy-paste — reference QuizShell line by line but adjust for Blueprint.

    Key differences from QuizShell:
    1. **Store**: Use `useBlueprintStore` instead of `useQuizStore` — all store calls reference Blueprint store
    2. **Questions**: Use `getBlueprintQuestions(parentStatus)` instead of QUESTIONS constant. The question array is dynamic based on parentStatus.
    3. **Section palette**: Use `getBlueprintSectionColor()` instead of `getSectionColor()`
    4. **Parent status gating**: When `parentStatus === null` (from store), render `<ParentStatusSelector>` instead of the intro screen. Once selected, store the status and show the email/intro screen.
    5. **Intro screen (step 0)**: Blueprint-flavored intro with teal accent instead of plum:
       - Headline: "Let's explore your parenting instincts" (per CONTEXT.md warm & curious tone)
       - Subtext: "You'll reflect on questions about how you parent (or imagine parenting). There are no right answers."
       - Email input field always shown, pre-fill attempt from Mirror localStorage (best-effort — Mirror partializes away email, so it likely won't be there. Try `localStorage.getItem("quiz-session")` -> `state.email`, fail silently if not found per RESEARCH.md)
       - CTA button: teal bg (#0F5845 or similar) instead of dark blue (#002833)
    6. **API route**: POST/PATCH to `/api/bp-quiz-session` instead of `/api/quiz-session`
       - POST body includes `parentStatus` field
       - No UTM params needed (pay-first flow, not organic acquisition)
    7. **Closing screen**: Briefer transition per CONTEXT.md -- "Your results are ready" momentum, less emotional than Mirror's "Thank you for reflecting":
       - Headline: something like "Your instincts tell a story" or "Your parenting style is taking shape" (Claude's discretion)
       - Button: "See my results"
       - Teal accent instead of plum
    8. **Completion redirect**: Navigate to `/blueprint/result?session=${sessionId}` instead of `/result?session=${sessionId}`
    9. **Processing screen**: Reuse existing `<ProcessingScreen />` component. If the amber dots feel off-brand, the teal adaptation is Claude's discretion — but reusing the existing component as-is is acceptable.
    10. **Gradient wash**: Adapt the radial gradient in the intro/closing screens to use teal-ish tints instead of yellow/pink (e.g., rgba(178,229,216,0.18) instead of rgba(254,244,172,0.18))

    Step flow (same structure as Mirror, with parent status prepended):
    ```
    parentStatus === null    -> show ParentStatusSelector (no step in URL)
    parentStatus !== null && step === 0  -> show Blueprint intro/email screen
    step 1..TOTAL             -> question screens (getBlueprintQuestions(parentStatus)[step-1])
    step TOTAL + 1            -> closing transition screen
    step >= TOTAL + 2         -> processing screen
    ```

    SSR-safe hydration: Use the exact pattern from QuizShell -- `useState(false)` + `useEffect` subscribing to `useBlueprintStore.persist.onFinishHydration()`. Return blank `<div className="min-h-screen" />` until hydrated.

    nuqs step sync: Same useQueryState pattern with `history: "push"`.

    Auto-advance: Same 300ms delay pattern as QuizShell.

    Back button: Same fixed-position back arrow, but only when step > 1 and within question steps.

    Slide animations: Reuse existing `animate-slide-in-right` / `animate-slide-in-left` from globals.css.

    Section headers: Use `<QuizSectionHeader>` component, passing Blueprint section palette colors.

    Scoring on completion: Call `computeDimensionProfile(store.answers, getBlueprintQuestions(store.parentStatus!))` then `getResult(dimensionProfile)` — same two-step pipeline as Mirror.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit && npx biome check components/quiz/ParentStatusSelector.tsx components/quiz/BlueprintQuizShell.tsx</automated>
    <manual>Verify BlueprintQuizShell uses useBlueprintStore (not useQuizStore), calls /api/bp-quiz-session, and renders ParentStatusSelector when parentStatus is null</manual>
  </verify>
  <done>
    ParentStatusSelector renders 3 parent-status options as tappable cards. BlueprintQuizShell orchestrates the full Blueprint quiz flow: parent status gating -> email capture -> questions with teal accent -> closing -> processing -> redirect to /blueprint/result. Uses Blueprint Zustand store, Blueprint questions, Blueprint section palette. Compiles and lints clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Blueprint quiz page route</name>
  <files>app/blueprint/quiz/page.tsx</files>
  <action>
    Create `app/blueprint/quiz/page.tsx` following the exact pattern from `app/quiz/page.tsx`:

    - Server Component that renders BlueprintQuizShell inside NuqsAdapter + Suspense boundary
    - Import `NuqsAdapter` from `nuqs/adapters/next/pages` (same as Mirror quiz page)
    - Import `BlueprintQuizShell` from `@/components/quiz/BlueprintQuizShell`
    - Suspense fallback: `<div className="min-h-screen bg-[#FAFAF7]" />` (cream, not amber — Blueprint doesn't use amber)
    - Export metadata with Blueprint-specific title and description:
      - title: "The Blueprint Quiz | Kin"
      - description: "Discover your parenting archetype -- your instincts, patterns, and approach to raising your child."
      - openGraph URL: "https://meetkin.com/blueprint/quiz"

    The existing `/blueprint` page (coming-soon waitlist) is NOT modified — it stays as-is per RESEARCH.md recommendation. The quiz lives at `/blueprint/quiz` as a sibling route.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx tsc --noEmit && npx biome check app/blueprint/quiz/ && npm run build</automated>
    <manual>Verify /blueprint/quiz route exists in build output and /blueprint page is unmodified</manual>
  </verify>
  <done>
    /blueprint/quiz route renders BlueprintQuizShell with NuqsAdapter + Suspense. Production build passes. Existing /blueprint coming-soon page is untouched.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npx biome check components/quiz/ app/blueprint/` passes with 0 errors
- `npm run build` passes — /blueprint/quiz route prerendered or marked dynamic
- ParentStatusSelector has 3 options matching CONTEXT.md spec
- BlueprintQuizShell uses `useBlueprintStore` (never `useQuizStore`)
- BlueprintQuizShell calls `/api/bp-quiz-session` (never `/api/quiz-session`)
- BlueprintQuizShell passes Blueprint questions to `computeDimensionProfile()`
- BlueprintQuizShell redirects to `/blueprint/result?session=` on completion
- Existing /quiz and /result Mirror routes are completely unaffected
</verification>

<success_criteria>
- Parent-status selector shows 3 warm, tappable options before quiz begins
- Full Blueprint quiz flow works: status selection -> email -> ~20 questions with teal accent -> closing -> processing -> result redirect
- Quiz state persists to "blueprint-quiz-session" localStorage key
- Leaving mid-quiz and returning restores progress
- Mirror quiz state at "quiz-session" is never touched
- Production build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-blueprint-quiz-engine/07-02-SUMMARY.md`
</output>
