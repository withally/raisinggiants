---
phase: 06-brand-positioning-research-and-visual-direction
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - .planning/phases/06-brand-positioning-research-and-visual-direction/06-05-landing-page-execution-plan.md
autonomous: false
requirements:
  - BRAND-08

must_haves:
  truths:
    - "Every visual direction recommendation from 06-04 is mapped to specific landing page files with what to change"
    - "The execution plan accounts for CURRENT codebase state — partial modifications already in place from visual refresh"
    - "Implementation is scoped to LANDING PAGE ONLY — no quiz, no result page, no other routes"
    - "Each implementation wave has specific file changes, what to keep vs. change, and verification steps"
    - "The user has reviewed and approved the overall Phase 6 direction"
  artifacts:
    - path: ".planning/phases/06-brand-positioning-research-and-visual-direction/06-05-landing-page-execution-plan.md"
      provides: "File-by-file landing page execution plan with phased implementation waves"
      contains: "Wave"
  key_links:
    - from: "06-04-visual-direction-brief.md"
      to: "06-05-landing-page-execution-plan.md"
      via: "Visual direction brief provides the spec; execution plan maps it to specific landing page files"
      pattern: "HeroSection|KOLCredibility|globals.css"
    - from: "06-05-landing-page-execution-plan.md"
      to: "future rebrand implementation phase"
      via: "Execution plan is the developer work order for the actual landing page implementation"
      pattern: "Wave [123].*files"
---

<objective>
Map every visual direction recommendation from 06-04 to specific landing page codebase files and produce a developer-ready execution plan with phased implementation waves, then get user approval of the complete Phase 6 direction.

Purpose: Phase 6 ends with a production-ready visual direction brief AND a file-by-file execution plan scoped to the landing page only. This plan produces the change matrix that a future implementation phase will execute. The checkpoint ensures the user reviews and approves the full strategic direction before implementation begins.

Output: `06-05-landing-page-execution-plan.md` — landing page execution plan with file mapping and phased waves.

CRITICAL SCOPE: Landing page ONLY. No quiz page. No result page. No other routes. Files in scope: `app/page.tsx`, `app/globals.css`, `app/layout.tsx`, `components/landing/*.tsx`, `components/ui/hand-drawn.tsx`, `components/ui/scroll-reveal.tsx`.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/06-CONTEXT.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/06-04-SUMMARY.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/HANDOFF.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/2026-TREND-RESEARCH-EXTRACTED.md

Landing page section order (from app/page.tsx):
1. HeroSection
2. SocialProof
3. ArchetypePreview
4. KOLCredibility
5. KOLNarrative
6. FAQ
7. SecondaryHero
8. Footer
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file-by-file landing page execution plan with current state audit and phased waves</name>
  <files>.planning/phases/06-brand-positioning-research-and-visual-direction/06-05-landing-page-execution-plan.md</files>
  <action>
Create the landing page execution plan document. This REPLACES the deleted 06-05-rebrand-execution-plan.md (note: different filename — the new one is `06-05-landing-page-execution-plan.md` because it is scoped to landing page only).

**CRITICAL: Audit current codebase state FIRST.**

The codebase has UNCOMMITTED changes from a partial visual refresh. Before specifying what to change, the executor MUST understand what has ALREADY changed. Run these commands:

```bash
# See all uncommitted file modifications
git diff --stat HEAD -- components/landing/ app/globals.css app/layout.tsx components/ui/

# Check what hand-drawn components exist
cat components/ui/hand-drawn.tsx

# Check PP Pangaia font loading
grep -A 20 "pangaia" app/layout.tsx

# Check CSS custom properties and new utilities
grep -n "ease-organic\|grain\|breathe\|glass-card" app/globals.css

# Check section padding patterns
grep -n "py-2[0-9]\|py-3[0-9]" components/landing/*.tsx
```

---

**Section 1: Current Codebase State (What Already Exists)**

Document what the partial visual refresh has ALREADY implemented. The executor must know what to KEEP vs. what to CHANGE.

**Already implemented (KEEP):**
- PP Pangaia loaded with 6 weights (Ultralight, Ultralight Italic, Medium, Medium Italic, Bold, Bold Italic) in `app/layout.tsx`
- `--ease-organic: cubic-bezier(0.22, 1, 0.36, 1)` CSS variable in `app/globals.css`
- `.grain` CSS class with feTurbulence noise overlay in `app/globals.css`
- `animate-breathe` CSS utility for decorative pulse in `app/globals.css`
- `.glass-card` CSS utility for glassmorphism cards in `app/globals.css`
- `.section-deep` CSS class for dark-on-teal token overrides in `app/globals.css`
- `animate-fade-up` CSS utility in `app/globals.css`
- Kin brand palette semantic tokens fully configured in `app/globals.css` (Cloud White, Espresso, Deep Teal, Merlot, Warm Sand, Burnished Amber)
- `components/ui/hand-drawn.tsx` with 3 components: HandDrawnDivider, HandDrawnQuoteMark, HandDrawnCircle
- `components/ui/scroll-reveal.tsx` updated with `distance` and `duration` props, organic easing, prefers-reduced-motion support
- Metadata updated to Kin branding in `app/layout.tsx` (title, OG, twitter)
- "Finally, the words for it." tagline in hero and metadata
- Hand-drawn elements integrated into: HeroSection (HandDrawnCircle eyebrow), SocialProof (HandDrawnQuoteMark), KOLCredibility (HandDrawnDivider between cards), KOLNarrative (HandDrawnDivider + custom vertical SVG line), FAQ (HandDrawnDivider between items), SecondaryHero (HandDrawnDivider end-mark), Footer (grain class)
- HeroSection: Ultralight display font, large type scale (140px XL), radial gradient decoration, increased padding (py-28 lg:py-40)
- SecondaryHero: Deep teal gradient background (linear-gradient), PP Pangaia display, Merlot CTA, breathing glow
- KOLCredibility: Featured 2 + supporting 6 bento-like layout (first 2 researchers span 2 cols), sketch portraits imported
- ArchetypePreview: Organic border-radius per pill (organicRadius function), grain on sample question card, subtle rotation (-0.5deg)
- SocialProof: HandDrawnQuoteMark in quote cards, Merlot-tinted marks

**Partially implemented (NEEDS REFINEMENT per 06-04 v4):**
- Section padding: Most sections at `py-24 lg:py-32`, but 06-04 recommends `py-28 lg:py-36` for more spaciousness
- KOLCredibility grid: Has 2-col feature + 4-col grid, but not truly asymmetric bento. Needs varied card sizing, not just first-2-span-2
- Type hierarchy: Some sections use `font-bold` where `font-semibold` or `font-extralight` would better serve the "one bold moment" principle
- Hand-drawn density: Some sections (KOLCredibility) use HandDrawnDivider on EVERY card — too dense. Needs to follow "2-4 per viewport" rule
- Color gradient usage: SecondaryHero has gradient but hero only has a faint radial blob. More intentional gradient storytelling needed
- Illustration: SecondaryHero references `secondary-hero-decoration.png` which may be a 404. Needs replacement or removal
- Max-width variation: All sections use max-w-5xl or max-w-6xl uniformly. Need narrower max-width (max-w-3xl) for intimate moments (SocialProof, testimonials)

**Not yet implemented (NEW WORK per 06-04 v4):**
- Additional hand-drawn components: HandDrawnUnderline, HandDrawnArrow, HandDrawnStar (add to hand-drawn.tsx)
- Hero headline word emphasis (HandDrawnUnderline on a key word)
- Narrower max-width for SocialProof/testimonial section (centered emotional moment pattern)
- Staggered card layout for ArchetypePreview (slight vertical offsets, #2 Brutalist whisper)
- Grain applied selectively (hero + 1-2 key sections, not every section)
- Color storytelling through scroll: Cloud White → Deep Teal → Merlot warmth progression
- Full-bleed moments (select sections breaking the container)

---

**Section 2: File-by-File Change Matrix**

For each file, document: current state, what to keep, what to change, estimated effort.

| File | What to Keep | What to Change | Effort |
|------|-------------|----------------|--------|
| `app/globals.css` | All existing utilities, palette tokens, grain, breathe, glass-card, section-deep | No changes needed — foundation is complete | None |
| `app/layout.tsx` | PP Pangaia 6-weight loading, metadata, Geist sans | Remove `secondary-hero-decoration.png` reference if it 404s (it's in SecondaryHero.tsx, not layout) | None |
| `components/ui/hand-drawn.tsx` | HandDrawnDivider, HandDrawnQuoteMark, HandDrawnCircle | ADD: HandDrawnUnderline (wavy underline for key phrases), HandDrawnArrow (organic CTA hint), HandDrawnStar (callout markers). Same stroke weight (1.2-1.5px), organic curve language, currentColor default | Moderate |
| `components/ui/scroll-reveal.tsx` | All current functionality, distance/duration props, organic easing, reduced-motion | No changes needed — component is complete | None |
| `components/landing/HeroSection.tsx` | Ultralight display, 140px scale, radial gradient blob, HandDrawnCircle eyebrow, increased padding, CTA | 1. Add HandDrawnUnderline under "words" in headline. 2. Consider removing the radial gradient blob (it's decorative noise — #5 says ONE bold moment, and the type IS the moment). 3. Verify padding is py-28 lg:py-40 (already correct). 4. Widen max-width for more dramatic left-aligned composition (max-w-7xl instead of max-w-6xl) | Moderate |
| `components/landing/SocialProof.tsx` | HandDrawnQuoteMark, ScrollReveal stagger, Merlot-tinted marks | 1. Narrow max-width from max-w-5xl to max-w-3xl for "centered emotional moment" pattern per 06-04. 2. Increase quote text size (text-base or text-lg italic PP Pangaia — this is a "pause in the scroll" moment). 3. Consider single-column layout on all screens (stacked quotes, not 3-col grid) for intimacy. 4. Reduce counter visual weight — it competes with the quotes | Moderate |
| `components/landing/ArchetypePreview.tsx` | Organic border-radius, grain on sample card, subtle rotation, HandDrawnCircle | 1. Add slight vertical stagger to archetype pills (CSS translateY offsets, #2 Brutalist whisper). 2. Increase section padding to py-28 lg:py-36. 3. Verify "one bold moment" = the headline, not the pill cloud | Small |
| `components/landing/KOLCredibility.tsx` | Sketch portraits, researcher data, HandDrawnDivider | 1. REDUCE hand-drawn density: remove HandDrawnDivider from every card. Use it only as section-level element (2-4 per viewport rule). 2. Make bento layout truly asymmetric: vary card heights, consider 3-col + 1-col tall layout instead of uniform 4-col. 3. Increase section padding to py-28 lg:py-36. 4. Left-align section header (already done — verify) | Significant |
| `components/landing/KOLNarrative.tsx` | Hand-drawn vertical SVG lines on blockquotes, PP Pangaia italic quotes, HandDrawnDivider | 1. This section is well-aligned with 06-04 direction (intimate, craft-detailed, emotionally led). 2. Increase padding if needed. 3. Verify max-w-3xl for intimate framing (already set) | Small |
| `components/landing/FAQ.tsx` | HandDrawnDivider between FAQs, ScrollReveal stagger | 1. Increase section padding to py-28 lg:py-36. 2. Section is well-implemented — minimal changes. 3. Verify max-w-2xl (already set — good for intimacy) | Trivial |
| `components/landing/SecondaryHero.tsx` | Teal gradient background, PP Pangaia display, Merlot CTA, breathing glow, HandDrawnDivider | 1. REMOVE or REPLACE the Image reference to `secondary-hero-decoration.png` (likely 404). Either remove entirely (let type be the composition per #5) or replace with a hand-drawn decorative element. 2. Verify gradient is smooth (already linear-gradient, looks good). 3. Keep as full-bleed moment (no max-width container on background — already correct) | Moderate |
| `components/landing/Footer.tsx` | Grain class, Kin branding, privacy/terms/contact links | 1. Update contact email from `hello@raisinggiants.com` to appropriate Kin email (or leave as-is if domain hasn't changed). 2. Minimal changes needed — footer is clean | Trivial |
| `app/page.tsx` | Section order, all imports | No changes needed — section composition is correct | None |

---

**Section 3: Implementation Waves**

**Wave 1: Hand-drawn component library expansion + foundation cleanup (1 plan, ~20 min)**

Files: `components/ui/hand-drawn.tsx`, `components/landing/SecondaryHero.tsx`

What to do:
- Add 3 new SVG components to hand-drawn.tsx: HandDrawnUnderline, HandDrawnArrow, HandDrawnStar
- All new components follow same conventions: `currentColor` default, `className` prop, `aria-hidden="true"`, stroke width 1.2-1.5px, organic curves
- Fix SecondaryHero: remove or replace the `secondary-hero-decoration.png` Image reference (check if file exists at `public/images/illustrations/secondary-hero-decoration.png` — if 404, remove the Image block entirely and let the typography be the composition)
- Verify all existing hand-drawn components still render correctly

Verification: `npx tsc --noEmit` passes. Dev server shows all hand-drawn elements rendering correctly.

**Wave 2: Section refinement — spacing, density, layout adjustments (1 plan, ~30 min)**

Files: `HeroSection.tsx`, `SocialProof.tsx`, `ArchetypePreview.tsx`, `KOLCredibility.tsx`, `FAQ.tsx`

What to do:
- **All sections:** Update padding from `py-24 lg:py-32` to `py-28 lg:py-36` where not already done
- **HeroSection:** Add HandDrawnUnderline under "words" in headline. Consider removing radial gradient blob. Widen max-width to max-w-7xl for dramatic left-aligned composition.
- **SocialProof:** Narrow to max-w-3xl. Switch to single-column stacked quotes. Increase quote text size to text-base/text-lg italic PP Pangaia. Reduce counter visual weight (smaller, muted).
- **ArchetypePreview:** Add slight translateY stagger to archetype pills for organic feel.
- **KOLCredibility:** CRITICAL — reduce HandDrawnDivider density. Remove per-card dividers, keep only 1-2 section-level dividers. Make grid truly asymmetric bento (vary card sizes, not just first-2-span).
- **FAQ:** Increase padding only. Otherwise well-implemented.

Verification: Dev server visual check — each section has ONE bold moment, hand-drawn elements are sparse (2-4 per viewport), generous whitespace between sections.

**Wave 3: Visual verification checkpoint (checkpoint, ~5 min)**

Human verification of the complete landing page against the 06-04 visual direction brief target feel:
> "A premium product that clearly had a human behind it. Not the cold perfection of a fintech dashboard, not the rough chaos of a zine. Something closer to a beautifully designed book that happens to live on the web — intentional, warm, spacious, with moments that make you slow down."

Check:
1. Scroll through entire landing page at 1440px width
2. Scroll through at 375px mobile width
3. Confirm hand-drawn elements are visible but sparse
4. Confirm generous whitespace (sections breathe)
5. Confirm one bold typographic moment per section
6. Confirm no 404 images (SecondaryHero decoration)
7. Overall: does it feel like "a beautifully designed book that lives on the web"?

---

**Section 4: What is NOT in scope**

- No quiz page changes
- No result page changes
- No new pages or routes
- No new npm dependencies
- No brand illustration creation (that's a future creative brief)
- No domain change (user action)
- No logo/OG image redesign (separate task)
- No changes to `components/landing/portraits/` (sketch portraits are fine as-is)
- No changes to `components/landing/illustrations/` (existing illustrations stay)
- No changes to `components/landing/ProductLadder.tsx`, `HeroWithIllustration.tsx`, `ArchetypeShowcase.tsx` (these are not in the current page.tsx composition)

---

**Section 5: Risk Assessment**

| Risk | Mitigation |
|------|-----------|
| Hand-drawn density too high after changes | Follow strict "2-4 per viewport" rule. Less is more. |
| KOLCredibility bento layout breaks on mobile | Test 375px. Bento should collapse to simple stacked cards on mobile. |
| SecondaryHero decoration removal leaves section feeling empty | The typography + gradient + Merlot CTA should carry the section alone. Typography IS the composition (#5). |
| SocialProof single-column feels too long | Limit to 3 quotes (already the case). Single-column at max-w-3xl is intimate, not long. |
| Changes conflict with uncommitted git state | All changes build ON TOP of the existing uncommitted changes. No reverts needed. The partial refresh is foundation, this is refinement. |

---

**Section 6: Total Scope Summary**

- **Files modified:** ~8 component files + 1 UI component file
- **Files NOT modified:** globals.css (complete), layout.tsx (complete), scroll-reveal.tsx (complete), page.tsx (complete)
- **New files:** None (hand-drawn.tsx gets new exports, not a new file)
- **New dependencies:** None
- **Estimated implementation:** 2 plans (Wave 1 + Wave 2) + 1 checkpoint (Wave 3) = ~55 min total
- **Scope:** Landing page ONLY
  </action>
  <verify>
    <automated>grep -c "Wave\|HeroSection\|SocialProof\|KOLCredibility\|hand-drawn\|globals.css\|SecondaryHero" .planning/phases/06-brand-positioning-research-and-visual-direction/06-05-landing-page-execution-plan.md | awk '{if ($1 >= 10) print "PASS: wave and file references present"; else print "FAIL: missing wave/file references"}'</automated>
  </verify>
  <done>File-by-file change matrix covers all 12 landing page files with current state, what to keep, what to change, and effort estimate; three implementation waves defined (component expansion, section refinement, visual checkpoint); current codebase state fully audited with keep/change/new categorization; no actual implementation performed; scope explicitly limited to landing page only</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review and approval of Phase 6 direction</name>
  <files>.planning/phases/06-brand-positioning-research-and-visual-direction/06-05-landing-page-execution-plan.md</files>
  <action>
Present the complete Phase 6 strategic direction for user review. This is the FINAL gate before implementation begins.

**Phase 6 produced 5 deliverables + the trend research:**

1. **Competitive Landscape** (06-01): 16+ millennial parenting digital products profiled with positioning map showing the empty parent-centric/self-understanding quadrant

2. **Reference Brand Gallery** (06-02): 8-12 aspirational brands with pattern synthesis identifying what millennial parents trust

3. **Positioning Narrative** (06-03): Brand territory locked — "digital tools for parents who want to understand themselves." Brand name: Kin. Architecture: Branded House. Emotional core: "I always sensed this. Now I see it."

4. **Visual Direction Brief v4** (06-04): Color palette (LOCKED), PP Pangaia KEEP, "#5 Neo-Minimalism + #6 Emotion-Led, seasoned with #9 Raw Authenticity." Visible human craft as differentiator. Target feel: "A beautifully designed book that lives on the web."

5. **Landing Page Execution Plan** (06-05): File-by-file change matrix scoped to landing page only. 3 waves: component expansion, section refinement, visual checkpoint.

6. **2026 Trend Research** (extracted): The five user-selected trends from Behance 2026 Design Trends gallery that informed v4.

**Key decisions to confirm:**
- Brand territory: "digital tools for parents who want to understand themselves" — is this right?
- Brand name: Kin — confirmed?
- Visual direction: "#5 + #6, seasoned with #9" — does this feel like your brand?
- Typography: PP Pangaia KEEP (not switching to Canela/serif) — agreed?
- Hand-drawn craft as differentiator (reversing old "NOT hand-drawn" direction) — agreed?
- Landing page scope: refinement of partial refresh, not a rebuild — reasonable?

**How to verify — review the deliverables in `.planning/phases/06-brand-positioning-research-and-visual-direction/`:**
1. Read `06-04-visual-direction-brief.md` — Does the visual direction match what you want?
2. Read `06-05-landing-page-execution-plan.md` — Does the implementation scope make sense?
3. Scroll the current landing page (dev server) — Does the partial refresh feel like the right foundation to build on?

Resume signal: Type "approved" to close Phase 6, or describe specific issues with any deliverable.
  </action>
  <verify>User confirms approval or provides specific feedback on deliverables</verify>
  <done>User has reviewed and approved (or provided revision feedback for) the visual direction brief v4, the landing page execution plan, and the overall Phase 6 direction — confirming the "#5 + #6, seasoned with #9" formula, PP Pangaia typography, hand-drawn craft as differentiator, and landing-page-scoped implementation plan</done>
</task>

</tasks>

<verification>
- [ ] Current codebase state fully audited — partial refresh changes documented (what exists, what works)
- [ ] File-by-file change matrix covers all landing page files with keep/change/effort
- [ ] Three implementation waves defined: component expansion, section refinement, visual checkpoint
- [ ] Every 06-04 recommendation mapped to a specific file and change
- [ ] Hand-drawn density rule (2-4 per viewport) explicitly enforced in change specifications
- [ ] Scope explicitly limited to landing page only — no quiz, no result page
- [ ] SecondaryHero decoration.png 404 risk addressed
- [ ] No actual codebase changes made — this is a plan, not implementation
- [ ] User has reviewed and approved the complete Phase 6 direction
</verification>

<success_criteria>
Phase 6 is complete when the user approves the strategic direction. The output is a visual direction brief v4 (synthesizing brand positioning + 2026 trends) and a landing-page-scoped execution plan that a developer can pick up and implement without further creative decisions. Every file change is specified, every keep/change decision is documented, and the current codebase state is fully accounted for.
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-positioning-research-and-visual-direction/06-05-SUMMARY.md`
</output>
