---
phase: 06-brand-positioning-research-and-visual-direction
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - components/landing/HeroSection.tsx
  - components/landing/SocialProof.tsx
  - components/landing/ArchetypePreview.tsx
  - components/landing/KOLCredibility.tsx
  - components/landing/KOLNarrative.tsx
  - components/landing/FAQ.tsx
  - components/landing/SecondaryHero.tsx
  - components/landing/Footer.tsx
  - components/ui/hand-drawn.tsx
  - app/globals.css
autonomous: false
requirements:
  - BRAND-06
  - BRAND-07
  - BRAND-08

must_haves:
  truths:
    - "ALL 8 landing page components are rebuilt to match their 06-04 section specs — layout, typography, color, craft elements, and bold moment per section"
    - "HeroSection uses asymmetric left-aligned layout with PP Pangaia Ultralight headline as the sole compositional focus"
    - "SocialProof uses single-column stacked quotes (not 3-column card grid) with PP Pangaia Medium Italic quote text"
    - "KOLCredibility uses asymmetric bento grid layout (not rigid 2x4 grid) with organic border-radius cards"
    - "FAQ uses two-column layout on desktop (sticky left header, scrolling right content)"
    - "SecondaryHero headline has dramatic Ultralight-to-BoldItalic weight contrast as emotional climax"
    - "hand-drawn.tsx is expanded with HandDrawnUnderline and HandDrawnArrow components"
    - "Section padding increased globally to py-28/py-36 standard"
    - "No section follows the old centered-headline → subtitle → grid → CTA template pattern"
    - "Page builds and renders without errors — `next build` succeeds"
  artifacts:
    - path: "components/landing/HeroSection.tsx"
      provides: "Rebuilt hero with asymmetric layout and typography-as-composition"
      contains: "font-extralight|font-display|text-\\[140px\\]"
    - path: "components/landing/KOLCredibility.tsx"
      provides: "Rebuilt KOL section with asymmetric bento grid"
      contains: "grid-template|col-span|row-span|organicRadius"
    - path: "components/landing/FAQ.tsx"
      provides: "Rebuilt FAQ with two-column sticky-header layout"
      contains: "sticky|lg:grid-cols"
    - path: "components/ui/hand-drawn.tsx"
      provides: "Expanded hand-drawn SVG library"
      contains: "HandDrawnUnderline|HandDrawnArrow"
  key_links:
    - from: "06-04-visual-direction-brief.md"
      to: "All 8 components/landing/*.tsx files"
      via: "Each section spec in the brief maps 1:1 to a component rebuild"
      pattern: "Section.*spec"
    - from: "components/ui/hand-drawn.tsx"
      to: "All landing components"
      via: "Hand-drawn SVG components imported and used per section spec"
      pattern: "HandDrawn"
---

<objective>
Rebuild all 8 landing page components to match the 06-04 visual direction brief, translating section-by-section design specs into working React components.

Purpose: This is the execution plan that turns the visual direction brief into code. The current landing page repeats a SaaS template pattern across every section. This plan rebuilds each component with its specified composition: asymmetric hero, stacked quotes, bento KOL grid, two-column FAQ, typographic climax in SecondaryHero. The result is a landing page that feels like "a beautifully designed book that happens to live on the web."

Output: 8 rebuilt landing page components, expanded hand-drawn SVG library, updated global styles. Human verification checkpoint at end.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/06-04-visual-direction-brief.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/06-04-SUMMARY.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/2026-TREND-RESEARCH-EXTRACTED.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/HANDOFF.md

Read ALL current component files before rebuilding:
@components/landing/HeroSection.tsx
@components/landing/SocialProof.tsx
@components/landing/ArchetypePreview.tsx
@components/landing/KOLCredibility.tsx
@components/landing/KOLNarrative.tsx
@components/landing/FAQ.tsx
@components/landing/SecondaryHero.tsx
@components/landing/Footer.tsx
@components/ui/hand-drawn.tsx
@components/ui/scroll-reveal.tsx
@app/globals.css
@app/layout.tsx
@app/page.tsx

<interfaces>
<!-- Key existing interfaces the executor needs. -->

From components/ui/hand-drawn.tsx:
```typescript
interface HandDrawnProps {
  className?: string;
  color?: string;
}
export function HandDrawnDivider({ className, color }: HandDrawnProps): JSX.Element;
export function HandDrawnQuoteMark({ className, color }: HandDrawnProps): JSX.Element;
export function HandDrawnCircle({ className, color, size }: HandDrawnProps & { size?: number }): JSX.Element;
// NEW components to add in Task 1:
// export function HandDrawnUnderline({ className, color }: HandDrawnProps): JSX.Element;
// export function HandDrawnArrow({ className, color }: HandDrawnProps): JSX.Element;
```

From components/ui/scroll-reveal.tsx:
```typescript
interface ScrollRevealProps {
  children: ReactNode;
  delay?: number;      // ms stagger delay
  className?: string;
  distance?: number;   // px translate distance (default 24)
  duration?: number;   // ms animation duration (default 800)
}
export function ScrollReveal(props: ScrollRevealProps): JSX.Element;
```

From components/landing/ArchetypePreview.tsx:
```typescript
// This pattern should be reused in KOLCredibility cards:
function organicRadius(index: number): string;
```

From app/globals.css (existing utilities):
```css
.grain { /* Grain texture overlay via ::after pseudo-element */ }
@utility animate-breathe { /* 6s breathing pulse */ }
@utility animate-fade-up { /* 0.8s organic fade-up */ }
:root { --ease-organic: cubic-bezier(0.22, 1, 0.36, 1); }
```

From app/layout.tsx:
```typescript
// PP Pangaia loaded with 6 weight/style combinations:
// 200 normal, 200 italic, 500 normal, 500 italic, 700 normal, 700 italic
// Available as: font-display class (via --font-display CSS variable)
// Weight classes: font-extralight (200), font-medium (500), font-bold (700)
// Italic: italic class
```

From components/landing/KOLCredibility.tsx:
```typescript
// Portrait components imported from ./portraits/SketchPortrait
import { BaumrindPortrait, GottmanPortrait, SiegelPortrait, AinsworthPortrait,
  KennedyPortrait, TsabaryPortrait, VanDerKolkPortrait, RohnerPortrait
} from "./portraits/SketchPortrait";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand hand-drawn SVG library and rebuild HeroSection, SocialProof, ArchetypePreview, KOLCredibility</name>
  <files>components/ui/hand-drawn.tsx, components/landing/HeroSection.tsx, components/landing/SocialProof.tsx, components/landing/ArchetypePreview.tsx, components/landing/KOLCredibility.tsx</files>
  <action>
**Step 1: Expand hand-drawn.tsx**

Add two new SVG components to `components/ui/hand-drawn.tsx`:

**HandDrawnUnderline** — A wavy underline SVG for emphasis on specific words. Similar width to HandDrawnDivider but thinner, meant to sit under inline text. viewBox "0 0 120 6", single organic wavy path, strokeWidth 1.0-1.2. Use as inline element under emphasized words.

**HandDrawnArrow** — An organic right-pointing arrow for CTA affordance. viewBox "0 0 24 16", hand-drawn arrow shape (not geometric/perfect). Slightly wonky, clearly hand-made. strokeWidth 1.5.

Both follow the existing `HandDrawnProps` interface pattern (`className`, `color` props).

---

**Step 2: Rebuild HeroSection.tsx**

Rebuild to match 06-04 Section 1 spec. The guiding principle: TYPE IS THE COMPOSITION. The headline is the entire visual experience.

Specific changes from current implementation:
- **Remove** the HandDrawnCircle import and its usage in the eyebrow
- **Layout:** Change from centered to LEFT-ALIGNED. Content block gets `max-w-6xl px-6 lg:px-16`. All text content constrained to `max-w-3xl` within the left portion. The right side is intentional whitespace.
- **Padding:** Increase from `py-28 lg:py-40` to `py-32 lg:py-44`
- **Eyebrow:** Keep "Kin — The Mirror" text, keep italic, keep font-display. Remove the HandDrawnCircle decorator. The text alone is sufficient.
- **Headline:** Keep "Finally, the words for it." Keep PP Pangaia Ultralight (font-extralight). Keep the size scale (text-6xl / sm:text-7xl / lg:text-[108px] / xl:text-[140px]). Ensure leading-[0.92] for tighter line height that makes the type feel more intentional.
- **Sub-copy:** Keep the single punchy line. Keep Warm Sand color. Keep max-w-xl.
- **CTA group:** Keep current structure. The rounded-full Deep Teal button stays. Consider adding a HandDrawnArrow component inline after the button text or as a separate element, but only if it doesn't clutter. If too busy, skip it.
- **Supporting copy:** Keep the ScrollReveal with the two paragraphs. Keep max-w-2xl.
- **Research badge:** Keep at bottom with border-t. Replace `border-t border-[#E8E4DF]` with `<HandDrawnDivider>` for consistency.
- **Decorative gradient blob:** Keep the teal radial gradient (top-right, desktop-only). Consider increasing size from w-[600px] to w-[800px] and making it more diffused (lower opacity values in gradient stops).
- **Grain:** Keep the `grain` class on the section.

Key principle: The hero is already close to the target direction. The main change is removing visual clutter (HandDrawnCircle eyebrow decorator) and ensuring asymmetric left alignment reads correctly. Do NOT over-engineer. The restraint IS the design.

---

**Step 3: Rebuild SocialProof.tsx**

Rebuild to match 06-04 Section 2 spec. The transformation: from a 3-column card grid to a single-column editorial quote sequence.

Specific changes:
- **Remove** the 3-column grid layout (`grid grid-cols-1 sm:grid-cols-3 gap-6`)
- **Counter:** Make the number dramatically larger. The CountUp component stays, but change sizing to `text-5xl sm:text-6xl font-bold` for the number. Keep "have already taken The Mirror" as a block element below (not inline).
- **Quotes layout:** Single column, max-w-2xl mx-auto, stacked vertically with space-y-8 or space-y-10.
- **Individual quotes:** REMOVE card styling (bg-white/80, border, rounded-2xl, p-5, shadow-sm). Replace with:
  - A `relative pl-6` wrapper
  - The hand-drawn vertical SVG line (reuse the pattern from KOLNarrative — an absolute-positioned SVG `left-0 top-0 h-full w-2` with an organic wavy vertical path in merlot at ~30% opacity)
  - Quote text: Switch from Geist (`text-sm`) to PP Pangaia Medium Italic (`font-display font-medium italic text-lg sm:text-xl`). Color: Espresso (#1A1008).
  - Attribution: Geist Sans, text-xs, Warm Sand. Keep the "— " prefix.
  - HandDrawnQuoteMark: Keep, but only on the FIRST quote (not all three — repetition dilutes the craft element).
- **CTA:** Keep the subtle text link. No changes.
- **Section padding:** Increase to `py-28 lg:py-36`.

---

**Step 4: Rebuild KOLCredibility.tsx**

Rebuild to match 06-04 Section 4 spec. The transformation: from rigid 2x4 grid to asymmetric bento layout.

Specific changes:
- **Remove** the uniform `grid grid-cols-2 lg:grid-cols-4 gap-3 lg:gap-4` layout.
- **New grid:** Use CSS Grid with explicit spans. Desktop layout (lg and above):
  ```
  grid-template-columns: repeat(5, 1fr)
  ```
  Card spans:
  - Baumrind (index 0): col-span-3, row-span-1 — featured, wide
  - Gottman (index 1): col-span-2, row-span-1 — featured, tall-ish
  - Siegel (index 2): col-span-2
  - Ainsworth (index 3): col-span-1
  - Kennedy (index 4): col-span-2
  - Tsabary (index 5): col-span-1
  - van der Kolk (index 6): col-span-2
  - Rohner (index 7): col-span-2

  On mobile (default): `grid-cols-1` — all cards stack single column.
  On sm: `grid-cols-2` — simple 2-column.

  Use Tailwind's grid classes: `lg:col-span-3`, `lg:col-span-2`, `lg:col-span-1`. Gap: `gap-3 lg:gap-4` (keep current).

- **Cards:** Add organic border-radius using the `organicRadius` function (import from shared location or inline). MOVE the `organicRadius` function out of ArchetypePreview.tsx into a shared location — either into `hand-drawn.tsx` (as a utility export) or keep it duplicated if simpler. The function is 5 lines; duplication is acceptable.
  - Apply `style={{ borderRadius: organicRadius(i) }}` to each card wrapper
  - Add hover effect: `hover:bg-white hover:-translate-y-0.5 hover:shadow-sm transition-all duration-200`
- **Keep:** HandDrawnDivider inside cards, portrait SVGs, all researcher data
- **Section header:** Keep left-aligned. Keep current structure.
- **Section padding:** Increase to `py-28 lg:py-36`.
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>hand-drawn.tsx expanded with HandDrawnUnderline and HandDrawnArrow. HeroSection rebuilt with left-aligned asymmetric layout, no eyebrow decorator, tighter leading. SocialProof rebuilt with single-column stacked quotes in PP Pangaia Medium Italic, no card grid. KOLCredibility rebuilt with 5-column bento grid and organic border-radius. All components render without build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild KOLNarrative, FAQ, SecondaryHero, Footer and update global styles</name>
  <files>components/landing/KOLNarrative.tsx, components/landing/FAQ.tsx, components/landing/SecondaryHero.tsx, components/landing/Footer.tsx, app/globals.css</files>
  <action>
**Step 1: Rebuild FAQ.tsx**

Rebuild to match 06-04 Section 6 spec. The transformation: from centered single-column to two-column with sticky header.

Specific changes:
- **Layout:** Replace the single `max-w-2xl` centered column with a two-column grid on desktop:
  ```
  <div className="mx-auto max-w-5xl px-6 lg:grid lg:grid-cols-[1fr_2fr] lg:gap-16">
  ```
  - Left column (1fr): Section header — eyebrow, headline "Before you begin", intro paragraph. On lg, add `lg:sticky lg:top-24 lg:self-start` so it stays visible while user scrolls through FAQs.
  - Right column (2fr): The FAQ items list.
  - On mobile (below lg): Single column, header on top, FAQ items below. Standard stacked layout.

- **Typography:** Reduce headline size slightly: `text-3xl sm:text-4xl` (instead of `text-4xl sm:text-5xl`). This is utility content. Remove `text-center` from header elements — left-align everything in the two-column layout.
- **FAQ items:** Keep current structure: question (font-semibold, Espresso), answer (Warm Sand, relaxed leading), HandDrawnDivider between items. No changes to the items themselves.
- **JSON-LD:** Keep the FAQ structured data script — SEO value.
- **Section padding:** Keep `py-24 lg:py-32` — slightly less than other sections because this is utility content.

---

**Step 2: Update KOLNarrative.tsx — minimal changes**

This section is ALREADY the most editorially correct. Minimal changes:
- **Spacing:** Increase `space-y-10` to `space-y-14` between quote blocks. Each quote should feel like turning a page.
- **Section padding:** Ensure consistency. Current uses `pb-24 lg:pb-32` (no pt because it shares bg with KOLCredibility). Consider adding `pt-4` or keeping as-is since HandDrawnDivider acts as separator.
- **Everything else stays.** The hand-drawn vertical SVG borders, the PP Pangaia Medium Italic quotes, the Geist Sans discovery/connection text — all already match the brief.

---

**Step 3: Rebuild SecondaryHero.tsx**

Rebuild to match 06-04 Section 7 spec. This is the emotional climax of the page.

Specific changes:
- **Remove** the Image import and the `secondary-hero-decoration.png` reference entirely. This image likely 404s and is visual clutter. The typography alone carries this section.
- **Remove** the `<div className="flex justify-center mb-6">` block containing the Image.
- **Padding:** INCREASE dramatically: `py-32 lg:py-48`. This section needs vast breathing room to feel like a book cover.
- **Content width:** Tighten to `max-w-2xl` (keep current).
- **Headline:** KEEP the current split — it already uses the Ultralight/BoldItalic contrast:
  - Line 1: `font-extralight` (200) — "You've always known."
  - Line 2: `font-bold italic` (700i) in Burnished Amber — "Now you'll see it."
  - The weight contrast IS the emotional arc. Keep sizing at `text-5xl sm:text-6xl lg:text-7xl`.
- **Supporting text:** Keep current copy and styling.
- **CTA:** Keep Merlot button. Ensure hover state includes shadow glow: `hover:shadow-xl hover:shadow-[#6B1E2E]/20` (already present).
- **Decorative:** KEEP the warm glow div (the radial gradient blur behind content). KEEP the HandDrawnDivider end-mark at bottom. Consider adding a very subtle large-scale organic SVG shape (a loose circle or arc) in Cloud White at ~5% opacity behind the headline — but ONLY if it enhances without competing. If uncertain, OMIT. Restraint over decoration.
- **Grain:** Consider adding `grain` class to section. Test visually — if the grain competes with the teal gradient, remove it. The gradient-to-grain combination can look muddy.

---

**Step 4: Update Footer.tsx — minimal changes**

- **Add** a HandDrawnDivider ABOVE the footer as a section separator. Since SecondaryHero and Footer share a dark color scheme, a divider helps delineate:
  ```tsx
  <div className="mx-auto max-w-5xl px-6 pb-4">
    <HandDrawnDivider color="rgba(245,244,242,0.12)" />
  </div>
  ```
  Place this inside the footer element, before the existing flex row.
- **Keep** everything else. The footer is already clean, confident, minimal.
- Import HandDrawnDivider if not already imported.

---

**Step 5: Global CSS updates in app/globals.css**

Review and ensure these exist (most already do from prior work):
- `.grain` class: Already exists. No changes.
- `--ease-organic`: Already exists. No changes.
- `animate-breathe`: Already exists. No changes.
- `animate-fade-up`: Already exists. No changes.

NO new CSS additions needed. All styling is handled via Tailwind utility classes in the components. The globals.css file is already in good shape.

---

**After all changes, run `npx next build` to verify no build errors.**
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>FAQ rebuilt with two-column sticky-header layout on desktop. KOLNarrative spacing increased. SecondaryHero cleaned up (image removed, padding increased, typography as sole focus). Footer gets HandDrawnDivider separator. All 8 landing components now match 06-04 brief specs. Build passes. No section follows the old template pattern.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual verification — landing page rebuild matches design intent</name>
  <files>None — visual review only</files>
  <action>
Run the development server (`npm run dev` or `npx next dev`) and review the landing page in a browser.

**Present to user for review against the 06-04 visual direction brief:**

Verification checklist for user:

1. **HeroSection:** Does the headline "Finally, the words for it." dominate the composition? Is the layout left-aligned with breathing room on the right? Is the eyebrow clean (no decorative circle)?
2. **SocialProof:** Are quotes stacked vertically (not in a 3-column grid)? Is the counter number dramatically large? Are quotes in PP Pangaia italic (not Geist)?
3. **ArchetypePreview:** Are archetype pills still visible with organic border-radius? Is the sample question card still the visual focus? Is the section header left-aligned?
4. **KOLCredibility:** Is the researcher grid asymmetric (bento layout with varied card sizes)? Do cards have organic border-radius? Do featured researchers (Baumrind, Gottman) have larger cards?
5. **KOLNarrative:** Is spacing between quotes generous (feeling like turning pages)? Are hand-drawn vertical borders visible on blockquotes?
6. **FAQ:** Is it two-column on desktop (sticky left header, scrolling right FAQs)? Does it stack to single column on mobile?
7. **SecondaryHero:** Is the Ultralight/BoldItalic headline contrast dramatic and emotional? Is the illustration image removed? Does the section feel like a "book cover" with generous spacing?
8. **Footer:** Is there a HandDrawnDivider above it? Is it minimal and confident?
9. **Overall:** Does the page feel like "a beautifully designed book that happens to live on the web — intentional, warm, spacious, with moments that make you slow down"? Does each section have a DISTINCT composition (not repeating the same template)?
10. **Mobile:** Check all sections on mobile viewport. Do layouts degrade gracefully to single column?

**If the user identifies issues:** Note them for follow-up adjustments (out of scope for this plan — would be a quick task or Phase 7 work).
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | grep -c "error" | awk '{if ($1 == 0) print "PASS: build has no errors"; else print "FAIL: build errors detected"}'</automated>
  </verify>
  <done>User has visually reviewed the rebuilt landing page against the 06-04 brief. All 8 sections have distinct compositions. The page reads as an editorial experience, not a SaaS template. Any user feedback documented for follow-up.</done>
</task>

</tasks>

<verification>
- [ ] All 8 landing page components rebuilt to match 06-04 section specs
- [ ] HeroSection: left-aligned asymmetric layout, no eyebrow decorator, PP Pangaia Ultralight headline dominates
- [ ] SocialProof: single-column stacked quotes, PP Pangaia Medium Italic, no card grid
- [ ] ArchetypePreview: left-aligned header, organic pills kept, sample card kept
- [ ] KOLCredibility: asymmetric bento grid (not 2x4), organic border-radius on cards
- [ ] KOLNarrative: increased spacing between quotes, hand-drawn borders kept
- [ ] FAQ: two-column on desktop with sticky left header, single-column on mobile
- [ ] SecondaryHero: image removed, massive padding, Ultralight/BoldItalic headline contrast
- [ ] Footer: HandDrawnDivider added above, minimal otherwise
- [ ] hand-drawn.tsx expanded with HandDrawnUnderline and HandDrawnArrow
- [ ] Section padding globally increased (py-28/py-36 standard where specified)
- [ ] `next build` succeeds with no errors
- [ ] No section follows centered-headline → subtitle → grid → CTA template pattern
- [ ] Color palette hex values unchanged (Cloud White, Espresso, Deep Teal, Merlot, Warm Sand, Burnished Amber)
- [ ] PP Pangaia used (no Canela, no DM Serif, no font changes)
- [ ] Landing page scope only — quiz and result page untouched
</verification>

<success_criteria>
The landing page is rebuilt. A visitor scrolling through the page experiences 8 distinct compositions — not 8 variations of the same template. The page feels like "a beautifully designed book that happens to live on the web." Each section has its own editorial identity while sharing the Kin brand palette, typography, and craft elements. The build passes. The user has verified the visual result.
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-positioning-research-and-visual-direction/06-05-SUMMARY.md`
</output>
