---
phase: 06-brand-positioning-research-and-visual-direction
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - components/landing/HeroSection.tsx
  - components/landing/SocialProof.tsx
  - components/landing/ArchetypePreview.tsx
  - components/landing/KOLCredibility.tsx
  - components/landing/KOLNarrative.tsx
  - components/landing/FAQ.tsx
  - components/landing/SecondaryHero.tsx
  - components/landing/Footer.tsx
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - components/ui/hand-drawn.tsx
autonomous: false
requirements:
  - BRAND-06
  - BRAND-07
  - BRAND-08

must_haves:
  truths:
    - "Every landing page component is REBUILT from scratch per the 06-04 visual direction brief — not tweaked or restyled"
    - "The design system from design-system/MASTER.md and design-system/pages/landing.md is applied to the actual code"
    - "Typography loading in layout.tsx matches the 06-04 brief's chosen fonts — NOT hardcoded to PP Pangaia unless 06-04 explicitly chose it"
    - "globals.css is updated with the 06-04 palette, spacing system, and texture utilities"
    - "The frontend-design skill was invoked during component rebuilds for quality assurance"
    - "The rebuilt page looks and feels fundamentally different from the current SaaS template"
    - "Scope is landing page ONLY — no quiz, no result page changes"
    - "User has reviewed and approved the rebuilt page before Phase 06 is closed"
  artifacts:
    - path: "components/landing/HeroSection.tsx"
      provides: "Rebuilt hero section per 06-04 spec"
      contains: "export function HeroSection"
    - path: "app/globals.css"
      provides: "Updated palette, spacing, and texture from 06-04 design system"
      contains: "root|--"
  key_links:
    - from: "06-04-visual-direction-brief.md"
      to: "components/landing/*.tsx"
      via: "Section-by-section specs translate into component rebuilds"
      pattern: "HeroSection|SocialProof|KOLCredibility"
    - from: "design-system/MASTER.md"
      to: "app/globals.css"
      via: "Design system palette and tokens map to CSS variables"
      pattern: "COLORS|TYPOGRAPHY"
---

<objective>
Rebuild every landing page component from scratch based on the 06-04 visual direction brief and the skill-generated design system.

This is NOT "apply the new palette to the existing layout." This is a REBUILD. Each component's JSX, layout, and styling gets rewritten to match the 06-04 composition specs. Content and data (researcher names, FAQ questions, quote text, archetype names) are preserved. Everything else is new.

The executor MUST use the frontend-design skill during implementation to ensure distinctive, non-generic output. The executor MUST reference design-system/MASTER.md and design-system/pages/landing.md for palette, typography, and style tokens.

Scope: Landing page ONLY. No quiz. No result page.
</objective>

<execution_context>
@/Users/yikfaiivanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yikfaiivanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-brand-positioning-research-and-visual-direction/06-04-visual-direction-brief.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/06-04-SUMMARY.md
@design-system/MASTER.md
@design-system/pages/landing.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/2026-TREND-RESEARCH-EXTRACTED.md
@.planning/phases/06-brand-positioning-research-and-visual-direction/HANDOFF.md

Read the skills before implementing:
@.agents/skills/frontend-design/SKILL.md
@.agents/skills/ui-ux-pro-max/SKILL.md
@.agents/skills/vercel-react-best-practices/SKILL.md

Read ALL current files being rebuilt:
@components/landing/HeroSection.tsx
@components/landing/SocialProof.tsx
@components/landing/ArchetypePreview.tsx
@components/landing/KOLCredibility.tsx
@components/landing/KOLNarrative.tsx
@components/landing/FAQ.tsx
@components/landing/SecondaryHero.tsx
@components/landing/Footer.tsx
@app/globals.css
@app/layout.tsx
@app/page.tsx
@components/ui/hand-drawn.tsx
@components/ui/scroll-reveal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update foundation — globals.css, layout.tsx, hand-drawn.tsx — to match 06-04 design system</name>
  <files>app/globals.css, app/layout.tsx, components/ui/hand-drawn.tsx</files>
  <action>
Before rebuilding components, update the foundation layer to match the 06-04 visual direction brief and design-system/MASTER.md.

**globals.css:**
- Update CSS custom properties to match the 06-04 chosen palette (whatever colors 06-04 specified — these may be completely different from the current Deep Teal/Merlot/Cloud White)
- Add/update spacing tokens, gradient definitions, and texture utilities per the design system
- Add any new animation keyframes or easing functions the brief calls for
- Remove utilities that are no longer used

**layout.tsx:**
- Update font loading to match the 06-04 chosen typography
  - If 06-04 chose Google Fonts: switch from self-hosted PP Pangaia to next/font/google
  - If 06-04 kept PP Pangaia: no change needed
  - If 06-04 chose a different self-hosted font: update localFont paths
- Update metadata: ensure title/description reflect "Kin" brand name
- Update CSS variable names if the font family changed

**hand-drawn.tsx:**
- If 06-04 calls for hand-drawn elements: update/expand the SVG component library per the brief's specs
- If 06-04 moved away from hand-drawn: simplify or remove components that are no longer used
- Add any NEW hand-drawn components the brief specifies

**IMPORTANT:** Read the 06-04 brief FIRST. Do not assume what the palette/typography/style will be. The brief was generated fresh using the design skills.

Verify: `npx next build` passes (no broken imports or missing fonts).
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Foundation layer (globals.css, layout.tsx, hand-drawn.tsx) updated to match 06-04 design system. Build passes. Font loading matches chosen typography. CSS variables match chosen palette.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild all 8 landing page components per 06-04 section specs</name>
  <files>components/landing/HeroSection.tsx, components/landing/SocialProof.tsx, components/landing/ArchetypePreview.tsx, components/landing/KOLCredibility.tsx, components/landing/KOLNarrative.tsx, components/landing/FAQ.tsx, components/landing/SecondaryHero.tsx, components/landing/Footer.tsx, app/page.tsx</files>
  <action>
Rebuild each of the 8 landing page components from scratch per the 06-04 visual direction brief's section-by-section composition specs.

**For each component:**

1. Read the 06-04 brief's spec for that section
2. Read the current component to identify content/data to preserve (researcher data, FAQ questions, quote text, archetype names, etc.)
3. REWRITE the component JSX from scratch:
   - New layout per the brief's Layout spec
   - New typography per the brief's Typography spec (using the design system's chosen fonts)
   - New color usage per the brief's Color spec
   - New craft elements per the brief's Craft spec
   - Implement the ONE bold moment the brief describes
4. Apply frontend-design skill principles: distinctive, bold, non-generic, unforgettable
5. Apply vercel-react-best-practices: Server Components by default, no unnecessary client state

**Component rebuild order:**
1. HeroSection.tsx — the first impression, sets the tone
2. SocialProof.tsx — builds trust through voice
3. ArchetypePreview.tsx — previews the product experience
4. KOLCredibility.tsx — establishes authority
5. KOLNarrative.tsx — deepens the emotional connection
6. FAQ.tsx — handles objections
7. SecondaryHero.tsx — emotional climax and conversion
8. Footer.tsx — quiet close

**After all 8 components are rebuilt:**
- Update app/page.tsx if section order or imports changed
- Verify build: `npx next build`
- Verify dev server renders correctly: `npx next dev`

**Content preservation checklist:**
- [ ] All 8 researcher names and data preserved (KOLCredibility, KOLNarrative)
- [ ] All FAQ questions and answers preserved
- [ ] All 9 archetype names preserved (ArchetypePreview)
- [ ] All social proof quotes preserved
- [ ] Hero headline copy preserved (or updated per 06-04 if it specifies new copy)
- [ ] All CTAs point to /quiz
- [ ] Brand name is "Kin" throughout

**Quality check — apply frontend-design principles:**
After rebuilding all components, do a full-page review:
- Does each section feel DIFFERENT from its neighbors? (Rhythm)
- Is there at least one moment that makes you stop scrolling? (Bold moment)
- Does it feel like "a beautifully designed book"? (Target feel)
- Is the typography doing real work, not just sitting there? (Type IS the composition)
- Would this be mistaken for any other parenting brand's page? (Distinctiveness)
  </action>
  <verify>
    <automated>cd /Users/yikfaiivanli/Projects/raisinggiants && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 landing page components rebuilt from scratch per 06-04 specs. Content preserved. Build passes. Each section has a distinct composition. The page looks and feels fundamentally different from the SaaS template.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — user reviews rebuilt landing page</name>
  <files>components/landing/HeroSection.tsx</files>
  <action>
Present the rebuilt landing page for user review.

**Take screenshots at both viewports:**
- Desktop: 1440px width, full page
- Mobile: 375px width, full page

Use the browser skill to capture these if available, or instruct the user to check localhost:3000.

**Present to user:**

The landing page has been rebuilt based on the visual direction brief. Here's what changed:

**Design system:**
- Palette: [what was chosen and why]
- Typography: [what was chosen and why]
- Style: [what style was applied]

**Section-by-section changes:**
[Summary of what each section looks like now vs. before]

**Questions for review:**
1. Does the page feel like "a beautifully designed book that lives on the web"?
2. Does each section feel different from its neighbors?
3. Is there a moment that makes you stop scrolling?
4. Does it feel distinctively "Kin" — not generic, not template?
5. What needs to change?

Type "approved" to close Phase 6, or describe what needs to change.
  </action>
  <verify>User confirms approval or provides specific feedback</verify>
  <done>User has reviewed and approved the rebuilt landing page, or provided specific feedback for iteration.</done>
</task>

</tasks>

<verification>
- [ ] globals.css updated with 06-04's chosen palette and design tokens
- [ ] layout.tsx font loading matches 06-04's chosen typography
- [ ] ALL 8 landing page components rebuilt from scratch (not just restyled)
- [ ] Content preserved (researcher data, FAQ content, archetype names, quotes)
- [ ] Build passes (`npx next build`)
- [ ] frontend-design skill principles applied (distinctive, bold, non-generic)
- [ ] Each section has a distinct composition (not repeating the same pattern)
- [ ] Scope is landing page ONLY (no quiz or result page changes)
- [ ] User has reviewed and approved the result
- [ ] Brand name "Kin" used throughout
</verification>

<success_criteria>
A user scrolls through the rebuilt landing page and it feels fundamentally different from the SaaS template. The palette, typography, and spatial composition were driven by the design skills and 2026 trend research — not inherited from old plans. Each section has its own identity. The page feels like "a beautifully designed book that lives on the web." The user approves.
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-positioning-research-and-visual-direction/06-05-SUMMARY.md`
</output>
